# Spoor 2: het rapport zonder begeleiding, en de vervolgmeting

Datum: 2026-09-16
Status: concept, wacht op review Lars
Bronnen: strategisch besluit 2026-09-11 ("de HR-manager is de facilitator, het rapport is haar script"); koude leesrondes `docs/rapport-koude-leesronde-2026-09-12.md` en `docs/rapport-koude-leesronde-2026-09-16.md` (delta: 1 opgelost, 25 open, 11 nieuw); stresstest `docs/rapport-stresstest-2026-09-10.md` (matrix na ronde 2 en de drie nieuwe open punten); ronde 3 (B9, B13, B14, B20); besluit Lars 2026-09-16 dat de vergelijkingspagina erin komt (optie A).

## 1. Doel en lat

Een HR-manager die het rapport voor het eerst ziet leidt er morgen een MT-vergadering van 45 minuten mee, zonder Loep in de kamer, en gaat naar buiten met een vastgelegd besluit. Bij een vervolgmeting ziet ze in één oogopslag wat er sinds dat besluit is veranderd.

**Lat, drie meetbare onderdelen:**
1. Stresstest: alle 21 scenario's zonder kruisje op Q1, Q2, Q5 en Q6; Q3 hoogstens twee tildes; Q4 (holle pagina's) zonder kruisje. Plus twee nieuwe scenario's: 22 (vervolgmeting op scenario 02) en 23 (vervolgmeting met gewijzigde afdelingen).
2. Koude leesronde 3 (zelfde persona, zelfde drie MT-reacties): nul blokkerende gaten, hoogstens vijf hinderlijke, en de drie reacties elk te pareren met een citaat van maximaal twee pagina's uit elkaar.
3. WeasyPrint-Docker: nul warnings; Python 3.11-guard groen; browsercheck op de testklant: campagne A downloadt en de besluitpagina print op één A4.

## 2. Ontwerpregels

- **Pagina twee is het vel dat het MT krijgt.** Alles wat de HR-manager in de eerste vijf minuten nodig heeft staat daar, met paginaverwijzingen naar het bewijs.
- **Eén verhaal over het startpunt.** Organisatiebreed en per afdeling mogen verschillende dingen aanwijzen, maar het rapport zegt dan zelf hoe die twee zich verhouden.
- **Van keuze naar besluit met werkvragen, niet met advies.** Loep verzint geen acties. Loep stelt de vragen die het MT moet beantwoorden om van "dit kozen je mensen" naar "dit gaan wij doen" te komen.
- **Het besluit is een product.** Het wordt vastgelegd in het rapport (invulbaar A4) en in het dashboard, en het komt terug in de vervolgmeting.
- **Elke telling heeft een noemer en elke drempel een uitleg**, op de plek waar hij voorkomt, niet alleen in de methodiek.
- Bestaande eerlijkheidsregels blijven: geen oorzaakclaims, geen individuen, staffels, geen automatische analyse van open tekst, geen em-dashes.

## 3. Scope in zeven onderdelen

| # | Onderdeel | Dekt |
|---|---|---|
| 1 | Pagina twee als MT-vel | leesronde B1, H1, H3, H4, H5, H8, H9, H10, H16, H17, C10, C11; ronde 2 open punt (a) |
| 2 | Eén startpuntverhaal | B2/B3 twee startpunten, H20 restgroep, C8 cover, ronde 2 open punt (b) |
| 3 | Werkvragen per startpunt | B4/B3 brug naar besluit, H6, H7 |
| 4 | Besluitpagina + dashboard | H11, H12, spec 11-9 par. besluitpagina |
| 5 | Vervolgmeting: koppeling + "Wat is er veranderd" | Sanne's zeven wensen, besluit optie A |
| 6 | Ronde 3 + restpunten | B9 paginavulling (H16, C5), B13 Anders, B14 brug tussen tellingen (H2, H19), B20 drempels (H18), ronde 2 open punt (c), C2, C3, C9, C12, C13, typografie |
| 7 | Taal | H14 jargon, H15 verspreidingsregel, terminologie onderwerp/thema/factor/stelling |

Loep Start blijft eerlijk gelabeld en krijgt geen werkvragen of vergelijking in deze spec (geen verdieping en richting, v1.1 apart). Loep Vertrek krijgt alles behalve de vergelijkingspagina in zijn eigen verleden-tijd-stem; de vergelijking geldt voor Behoud en Vertrek beide.

## 4. Onderdeel 1: pagina twee als MT-vel

Pagina twee wordt herbouwd als één A4 met vaste blokken, in deze volgorde:

1. **Kop: het antwoord in één zin**, zoals ronde 2 die al maakt (vier vormen naar aantal kwetsbare onderwerpen, vlak profiel, gedeelde laagste). Twee correcties: de zin over "één onderwerp" valt weg als het profiel meer dan één kwetsbaar onderwerp heeft (H17), en de vlakke-profiel-zin vergelijkt op de getoonde, afgeronde score (ronde 2 punt a). Bij een gelijke laagste score noemt de zin alle gelijke onderwerpen.
2. **De twee cijfers die het MT wakker maken.** Voor Behoud: blijfintentie met zone-verdeling ("Blijfintentie 3,9 op 10: 25 van de 39 zitten onder de 5, dat is kwetsbaar") en de responsbasis met oordeel ("39 van 58 ingevuld, 67%: ruim genoeg voor een betrouwbaar beeld" / de bestaande waarschuwingen onder 50% en 30%). De blijfintentie krijgt dezelfde bandlogica als de factoren (onder 5 kwetsbaar, 5 tot 6,5 aandachtspunt, boven 6,5 relatief sterk) en wordt in de kop benoemd zodra hij kwetsbaar is. Voor Vertrek: de meest genoemde vertrekreden met noemer, en bij een gelijkspel "twee redenen even vaak genoemd: X en Y" (ronde 2 punt b).
3. **Startpunt en waarom**, met de bestaande onderbouwingscel, maar de cellen die geen reden zijn (C10) verdwijnen: er staan alleen cellen met een echte reden (score, spreiding, verdieping, richting, vertrekreden). De regel "11 van de 15" krijgt zijn noemer ter plekke: "11 van de 15 mensen bij wie dit het laagst scoorde".
4. **Eén gespreksopener** (H9). De opener op pagina twee en op de gespreksagenda zijn dezelfde zin; de agenda verwijst naar pagina twee in plaats van een tweede te maken.
5. **Zo leid je dit gesprek in 45 minuten**: vijf regels met tijdvak, wat je op tafel legt, en de paginaverwijzing (pagina's worden in het rapport geteld en overal genoemd waar naar een sectie wordt verwezen, H4). Dit vervangt het gebruiksblok; de zin over de "begeleide managementbespreking" verdwijnt (H5).
6. **Meetgegevens**: meetperiode als datums (start en sluiting uit het delivery record, H8), aantal uitgenodigd, aantal ingevuld, en bij een vervolgmeting de datum van de vorige meting.

Pagina drie mag niet bijna leeg zijn (H16): het cijferoverzicht dat nu op pagina drie begint, sluit direct aan op pagina twee of pagina twee wordt zo gebouwd dat hij precies één A4 vult. De lay-outregel wordt getest met de drie langste factornamen en met scenario 11 (n=180).

## 5. Onderdeel 2: één startpuntverhaal

- Het navy segmentblok heet niet langer "Startpunt voor de bespreking" maar **"Waar het per afdeling begint"**. Het startpunt van het rapport is er één, organisatiebreed, op pagina twee en de gespreksagenda.
- Als de aangewezen afdeling een ander onderwerp laag heeft dan het organisatiebrede startpunt, zet het rapport op de gespreksagenda én op pagina twee de **brugzin**: "Organisatiebreed begint het gesprek bij [X]. Bij [afdeling] springt [Y] eruit ([score]); neem dat als tweede punt voor die afdeling." Als het hetzelfde onderwerp is: "Bij [afdeling] weegt [X] het zwaarst ([score]); daar begint het gesprek ook." Als geen afdeling wordt aangewezen (ronde 2-regels): geen brugzin, wel de bestaande eerlijke reden.
  - **Amendement (codereview plan 3a taak 7, defect 2): de brugzin is bandbewust.** "Springt eruit" en "neem dat als tweede punt" zeggen dat daar iets aan de hand is. Scoort het laagste thema van die afdeling relatief sterk (vanaf 6,5, de grens van `_factor_label`, geen nieuwe drempel), dan is dat onwaar: het is alleen het laagste van zes goede scores, en een opdracht eromheen zet het MT op een onderwerp dat geen aandacht vraagt. Het navy afdelingsblok koos in 2026-07-16 al bewust een band-neutrale formulering om precies die reden; de brugzin mag die keuze niet terugdraaien. Boven die grens luidt de zin daarom "Organisatiebreed begint het gesprek bij [X]. Het laagst scorende thema bij [afdeling] is [Y] ([score]), en dat scoort daar relatief sterk." en, bij hetzelfde onderwerp, "Bij [afdeling] is [X] het laagst scorende thema ([score]); daar begint het gesprek ook." Bij kwetsbaar punt en aandachtspunt blijft de sterke vorm staan.
  - **Amendement (defect 1): de variant zonder thema.** Haalt bij de aangewezen afdeling geen enkel thema de per-factor-gate, dan is er geen [Y] om te noemen. De zin gebruikt dan dezelfde kwalificatie als het navy blok ("scoort het laagst van de afdelingen die apart getoond worden") en noemt, net als dat blok, de gepoolde restgroep zodra die lager staat: anders wordt de claim op pagina twee weerlegd door de afdelingstabel verderop.
  - Beide onderwerpen in de brugzin staan met een hoofdletter: de zin noemt er twee, en de kernzin en de cover op dezelfde pagina schrijven het startpunt ook zo.
- De restgroep "Overige afdelingen" (H20) krijgt in de tabel een noemer ("samen X uitgenodigd, Y ingevuld") en de zin welke afdelingen erin zitten ("Facilitair, Staf"), zodat "scoort het laagst" niet over een naamloze groep gaat. Onder de vijf ingevulde blijft de restgroep zonder score, met reden.
- Cover, band en kop gebruiken één woord voor hetzelfde ding (C8): de cover zegt "Waar het gesprek begint: [X]" in plaats van "Eerste aandachtspunt", en de bandnamen blijven "kwetsbaar punt / aandachtspunt / relatief sterk".

## 6. Onderdeel 3: werkvragen per startpunt

Op de gespreksagenda, direct onder "Wat er moet gebeuren", komt per startpunt (en per tweede punt) een blok **"Zo maak je er een besluit van"** met drie vragen. Geen advies, wel de vertaalslag die het MT moet maken:

1. **Herkenningsvraag**, altijd datagedreven: "[N] van de [M] koos '[meest gekozen toelichting]'. Waar zie je dat bij jullie terug, en waar niet?" Bij te weinig verdiepingsdata: "Wat maakt dat [onderwerp] hier zo laag scoort, volgens jullie?"
2. **Vertaalvraag**, uit een vaste set per onderwerp en scan (`WORK_QUESTIONS` in `backend/products/shared/deepening.py`, naast de routesets), gekozen op de meest gekozen richting: bij "Een concreter gesprek over mijn ontwikkeling" bijvoorbeeld "Wat is bij jullie een concreet ontwikkelgesprek: wie voert het, hoe vaak, en wat moet eruit komen?" Bij een verdeelde richting (`divided`, `plurality`, `split_none`) wordt de vraag: "Je mensen zijn verdeeld tussen [A] en [B]. Welke van de twee past bij wat jullie de komende drie maanden kunnen waarmaken?"
3. **Besluitvraag**, altijd dezelfde vorm: "Wat spreken jullie vandaag af, wie is eigenaar, en waaraan zie je over 90 dagen dat het werkt?"

De vaste set: 36 vertaalvragen (6 onderwerpen × 6 routes) voor Behoud en 36 voor Vertrek in verleden-tijd-stem, als concept in bijlage A van deze spec; reviewgate door Lars zoals bij de routeteksten in juli. Loep Start krijgt de vragen niet (geen richtingdata).

De reactie van de afdelingsmanager ("dat komt door de reorganisatie") krijgt op de segmentpagina één vaste regel: "Een lage score zegt niet waarom. Vraag de afdeling zelf naar de toelichting; het rapport toont die alleen organisatiebreed." (H7, eerlijk over de grens.)

## 7. Onderdeel 4: de besluitpagina

De laatste pagina vóór de appendix wordt een **invulbaar A4 "Besluit van het MT"**, ook los te printen, met velden:

- Meting en datum van deze bespreking
- Startpunt (voorgedrukt) en **Wat precies** (drie regels)
- Eigenaar en **Datum vervolgmoment** (geen termijn, een datum; de hint "45 tot 90 dagen" staat ernaast)
- Tweede punt (voorgedrukt als er een is) met dezelfde drie regels
- **Terugkoppeling aan medewerkers**: wie, wanneer, wat
- **Waaraan zien we dat het werkt**: één regel
- Voetregel: "Vul dit ook in op je dashboard; bij een vervolgmeting zet Loep dit besluit op pagina twee."

**Dashboard:** op een gesloten meting met rapport komt een blok "Besluit vastleggen" met dezelfde velden, opgeslagen in een nieuwe tabel `campaign_decisions` (één rij per meting: `campaign_id`, `decided_at`, `primary_topic`, `primary_action`, `owner`, `follow_up_date`, `secondary_topic`, `secondary_action`, `feedback_plan`, `success_criterion`, `recorded_by`, `updated_at`; RLS als de audittabel: leden lezen, eigenaar en operator schrijven). Het formulier is de enige plek waar de klant iets in het systeem schrijft ná de meting; Fail Loud bij opslagfouten.

## 8. Onderdeel 5: de vervolgmeting

### 8.1 Koppeling

`campaigns.previous_campaign_id` (uuid, nullable, FK naar `campaigns`, zelfde organisatie afgedwongen met een check via trigger of in de server action). Lars kiest bij het aanmaken van de vervolgmeting de vorige meting in het beheerformulier (dropdown van gesloten metingen van dezelfde organisatie en hetzelfde scantype). Geen automatische gok. De cover toont "Vervolgmeting" met beide meetdata.

### 8.2 Pagina "Wat is er veranderd"

Direct ná pagina twee, alleen bij een gekoppelde vorige meting, één pagina:

1. **Jullie besluit van [maand jaar]**: uit `campaign_decisions` van de vorige meting: wat, wie, datum vervolgmoment. Ontbreekt het besluit: "Er is voor de vorige meting geen besluit vastgelegd in het dashboard." (Fail Loud, geen leeg vak.)
2. **De zes onderwerpen toen en nu**, in de volgorde van het cijferoverzicht: score toen, score nu, verschil. Verschil wordt alleen als verschuiving benoemd ("hoger", "lager") als het ten minste 0,3 is én beide metingen tien of meer antwoorden hebben; anders "vrijwel gelijk" of "niet vergelijkbaar (te weinig antwoorden in [meting])". Het startpunt van toen krijgt een markering.
3. **Blijfintentie toen en nu** met de drie zones (Behoud); vertrekintentie idem (Behoud); voor Vertrek de vertrekredenverdeling toen en nu.
4. **De toelichting en de richting bij het startpunt van toen**: aandeel toen en nu voor de meest gekozen toelichting en voor de meest gekozen richting, met noemers. Dat is de enige plek die iets zegt over "heeft het besluit iets gedaan", en het rapport zegt er expliciet bij dat dit geen oorzaakverband bewijst.
5. **Respons toen en nu** met uitgenodigden; een daling van meer dan 15 procentpunt krijgt de zin "Dat is zelf een signaal."
6. **Afdelingen**: alleen afdelingen die in beide metingen bestaan (op label), met laagste onderwerp toen en nu; nieuwe of verdwenen afdelingen worden genoemd, niet vergeleken.

**Eerlijkheidsregels op de pagina zelf:** "Andere mensen hebben ingevuld dan vorige keer; dit vergelijkt groepen, geen personen." En: "Een verschil onder 0,3 is ruis bij deze aantallen."

### 8.3 Pagina twee bij een vervolgmeting

Blok 2 van pagina twee krijgt een extra regel: "Vorige meting: [startpunt toen], besluit: [wat precies]. Zie pagina 3 voor wat er veranderde."

## 9. Onderdeel 6: ronde 3 en restpunten

- **B9 paginavulling**: geen pagina onder 40% gevuld. eNPS krijgt geen eigen pagina meer (H13: op de pagina met de intentiecijfers); halflege pagina's 9, 11, 12 worden samengevoegd of aangevuld; pagina 3 (H16) volgens onderdeel 1.
- **B13 Anders-toelichtingen**: als "Anders" bij een verdieping 20% of meer haalt, zegt het rapport dat en toont de geanonimiseerde teksten op de verdiepingspagina (staffel: vanaf vijf; anders alleen het aantal).
- **B14 brug tussen tellingen** (H2, H19): elke telling in de verdiepings- en richtingketen staat in één vaste vorm "X van de Y (Y = ...)", en de richtingketen sluit: "39 kregen de vraag, 27 beantwoordden hem, 12 sloegen over" staat er letterlijk, zodat er nooit twaalf mensen "verdwijnen".
- **B20 drempels** (H18): één drempeltabel op de methodiekpagina (3 voor richting, 5 per groep, 8 voor agendaverrijking, 10 voor patroonduiding, met één zin waarom elk), en elke inline drempel verwijst ernaar met dezelfde woorden.
- Ronde 2 punt (c): de kolomkoppen van de ranglijst in een `thead`, zodat ze herhalen op een vervolgpagina (te testen door de tabel geforceerd te laten breken).
- Cosmetisch: het anonimiseringslabel één keer boven de quotes (C2); de dubbele bandlijst weg (C3); afdelingstabel met kolomkoppen (C9); geen "(vervolg)" in de kop van een nieuw onderwerp (C12); "vrijwel gelijk aan" niet in de agendakolom (C13); "geïnteresseerd" met trema en losse streepjes weg.

## 10. Onderdeel 7: taal

- "Bestuurlijke read" wordt "Het antwoord in het kort"; "verdieptrigger", "interventieprescriptie", "responsbasis" en "managementread" verdwijnen uit klantcopy (H14). Vertaaltabel in bijlage B; source-guard-test op de verboden woorden.
- Eén woord per ding: "onderwerp" voor de zes gemeten thema's, "stelling" voor de losse vragen, "afdeling" voor segmenten. "Factor" en "thema" verdwijnen uit klantcopy.
- Cover en slotpagina krijgen één verspreidingsregel (H15): "Voor het MT en HR van [organisatie]. Deel dit rapport niet met individuele medewerkers; de toelichtingen zijn geanonimiseerd maar herkenbaar in kleine teams."

## 11. Data en migraties

Eén migratie, additief: `campaigns.previous_campaign_id`, tabel `campaign_decisions` met RLS, beide idempotent. Draaien vóór de Railway-redeploy. De backend leest `campaign_decisions` alleen via de bestaande sessie (service-role-equivalent), de frontend via RLS.

## 12. Verificatie

- Stresstest-harnas uitgebreid met scenario 22 (vervolgmeting op 02: startpunt verschoven, besluit vastgelegd) en 23 (vervolgmeting met een afdeling erbij en een weg, zonder vastgelegd besluit); de matrix krijgt een kolom Q7 "vergelijking eerlijk" voor die twee.
- Koude leesronde 3 door een agent op het nieuwe voorbeeldrapport én op scenario 22, met dezelfde drie MT-reacties.
- WeasyPrint-Docker op alle voorbeeldrapporten en op 06, 11, 18, 22, 23: nul warnings; kolomkoppen herhalen bij geforceerde paginabreuk.
- Python 3.11-guard groen; backend 25 = baseline; frontend tsc en vitest op baseline.
- Browsercheck testklant: campagne A downloadt (Railway-500 moet eerst weg), besluit vastleggen werkt, en een tweede testcampagne gekoppeld aan A toont de vergelijkingspagina.

## 13. Uitvoering

Drie plannen, na elkaar, elk in een aparte sessie (allemaal in `report_html.py`, dus niet parallel):

- **Plan 3a: pagina twee, startpuntverhaal, ronde 3, taal** (onderdelen 1, 2, 6, 7). Geen schemawijziging.
- **Plan 3b: werkvragen en besluitpagina** (onderdelen 3, 4), inclusief de reviewgate op de 72 vertaalvragen en de migratie voor `campaign_decisions`.
- **Plan 3c: vervolgmeting** (onderdeel 5), inclusief `previous_campaign_id`, beheerformulier, scenario 22 en 23.

Na 3a draait de stresstest opnieuw; na 3c de koude leesronde 3. De backend-responsnoemer uit spoor 1 (par. 4.6 van de spec van 11 september) zit in 3a.

## 14. Open punten voor de review

1. De vertaalvragen (bijlage A, 72 stuks) schrijf ik als concept nadat je deze spec goedkeurt; reviewgate zoals in juli. Akkoord met de vorm "herkenning, vertaling, besluit"?
2. Besluit vastleggen in het dashboard: nu meebouwen (3b), of eerst alleen het papieren A4 en het dashboard bij 3c? Mijn advies: meteen, anders heeft 3c niets om op pagina twee te zetten.
3. De cover-formulering "Waar het gesprek begint: [X]": akkoord, of liever de bestaande "Eerste aandachtspunt" met aangepaste bandnaam?

## Bijlage A: vertaalvragen (concept volgt na akkoord)

Per onderwerp (leiderschap, cultuur, groeiperspectief, beloning, werkdruk, rolhelderheid) en per route uit `DIRECTION_SETS` één vertaalvraag in de vorm "Wat is bij jullie een concrete [route]: wie, hoe vaak, wat moet eruit komen?", voor Behoud in de tegenwoordige tijd en voor Vertrek in de vorm "Wat had bij jullie een concrete [route] moeten zijn?".

## Bijlage B: vertaaltabel jargon

| Nu | Wordt |
|---|---|
| Bestuurlijke read | Het antwoord in het kort |
| responsbasis | respons |
| verdieptrigger | verdiepende vraag |
| interventieprescriptie | advies |
| managementread | samenvatting voor het MT |
| factor / thema | onderwerp |
| item | stelling |
| segment | afdeling |

## Afwijkingen bij plan 3a

- **Responsnoemer (spec 11-9 par. 4.6):** de code (`_respons_noemer`, ronde 2) neemt het vastgelegde aantal uit het delivery record als noemer ongeacht `comms_mode`, en geeft bij een te laag vastgelegd aantal géén afgekapte 100% maar géén percentage plus de reden. Dat is strenger dan par. 4.6 en blijft zo: liever geen getal dan een onwaar getal.
- **Sluitdatum:** `closed_at` staat op `Campaign`, niet op het delivery record; de start komt uit `delivery_record.launch_date`.
- **Respons-oordeel zonder noemer (blok 2, codereview taak 2):** het plan schreef "het aantal uitgenodigden is niet vastgelegd". Dat is onwaar bij een te laag vastgelegd aantal of een managed campagne waarin iedereen invulde (`_respons_noemer`). Wordt: "{n} ingevuld; Loep kan het aantal uitgenodigden niet vaststellen, dus staat er geen percentage." Onder `MIN_AGGREGATE_N` volgt ook hier de drempelmelding: "Te weinig voor een profiel per onderwerp, daarvoor zijn er minimaal 10 nodig."
- **Vertrekreden in blok 2 (codereview taak 2):** de noemer is het aantal respondenten dat een vertrekreden gaf (de reden is optioneel), niet het aantal ingevulde vragenlijsten. Is dat kleiner, dan staat het erbij: "(4 van de 10 die een reden gaven)", bij een gelijkspel "(elk 4 van de 10 die een reden gaven)". Het gelijkspel wordt bepaald op de volledige teller (`exit_r_top` in `build_report_data`), niet op de top 5 van de tabel; boven "zes" staat het cijfer.
- **Blijfintentie in de kop (codereview taak 2):** zonder kwetsbaar onderwerp in de kop wordt het "Wel is de blijfintentie kwetsbaar: ..." in plaats van "Ook ...", anders spreekt de zin "Geen onderwerp scoort kwetsbaar." tegen. Enkelvoud "1 van de 39 zit onder de 5"; zonder losse scores geen kopzin.
- **"Daar begint het gesprek." (taak 3, C11):** het plan verkortte de startpuntzin bij één of twee kwetsbare onderwerpen zodra het startpunt het eerstgenoemde is. Bij twee kwetsbare onderwerpen, of met een "Daarnaast zijn ... een aandachtspunt."-zin ertussen, wijst "Daar" dan naar meer dan het startpunt. De verkorting geldt daarom alleen bij precies één kwetsbaar onderwerp zonder aandachtspuntenzin; anders blijft "Als startpunt kiest Loep X." staan.
- **Getoonde gelijkstand in de startpuntzin (taak 3, codereview):** het verschil naar de volgende rij (`_p02_startpunt_gronden`) wordt berekend op de getoonde scores (B15), afgerond op 0,1. 4.94 en 4.96 staan als 4.9 en 5.0 in het raster; de zin zegt dan "klein, 0,1 punt" in plaats van "0,02 punt". Een getoond verschil van 0 wordt vanzelf de gelijkstandvorm. De gate "klein" (onder `PRIORITY_TIE_MARGIN`) is dezelfde maar werkt op dat getal, dus een getoond verschil van 0,3 is niet meer klein. Het getal in de zin heeft daarom één decimaal ("0,1 punt"). Geen nieuwe drempel.
- **Opsomming van gedeelde laagste scores (taak 3, codereview):** labels bevatten zelf al "en", dus geen "A en B" meer maar telwoord, dubbele punt en komma's. Tak zonder kwetsbaar onderwerp: "Geen onderwerp scoort kwetsbaar. Twee onderwerpen delen de laagste score (5.5/10): A, B; als eerste gesprekspunt kiest Loep X." Vlakke zin: "(laagste score 6.2/10, gedeeld door drie onderwerpen: A, B, C; hoogste D 6.9/10)"; bij één laagste blijft "(laagste A 6.0/10, hoogste D 6.5/10)". Plan-pins die hierdoor veranderen: in `test_report_p02_kernzin` de vlakke-zin-test ("laagste A, B en C 6.2/10") en de twee gedeelde-laagste-pins ("A en B delen de laagste score (5.8/10); ..."), en `test_report_p02_profielvorm::test_vlakke_zin_noemt_laagste_en_hoogste_met_scores`.
- **Alle onderwerpen dezelfde score (taak 3, codereview):** geen "laagste ... hoogste" (dan zou één onderwerp beide heten) maar "Geen enkel onderwerp springt eruit: alle zes onderwerpen scoren 6.2/10. Dat is zelf de bevinding." De startpuntzin krijgt dan geen gelijkstandvorm maar de kale keuze; de kop zegt de gelijkstand al.
- **Gelijkstandzin noemt het gelijke onderwerp (taak 3, codereview):** "deelt de laagste score met het volgende" wordt "met {naam}" bij één ander onderwerp en "met twee andere onderwerpen" (telwoord) bij meer. De namen komen uit het profiel (de onderwerpen op de laagste getoonde score), niet uit de volgende rasterrij: na een tie-break hoeft die niet de gelijke te zijn. "Laagste" voor het startpunt betekent nu: hoort bij de onderwerpen met de laagste getoonde score. Pins: `test_report_p02_kernzin::test_exacte_gelijkstand_krijgt_een_eigen_zin` en `test_report_respons_gevolgen::test_gelijkstandzin_wordt_een_mogelijk_startpunt` (fixture kreeg een echte getoonde gelijkstand, anders is er geen onderwerp om te noemen).
- **Eén verschilhelper voor alle drie de scans (taak 3, codereview 2):** `_getoond_verschil` rekent het verschil op de getoonde scores; Loep Start (`render_onboarding_report_html`) rekende dat nog zelf op ruwe scores en rendert anders "klein, 0,0 punt". De vlakke kop met alle onderwerpen gelijk laat de gelijkstandzin weg (die zegt het al); de kop "6 van de 6 onderwerpen scoren kwetsbaar" niet, dus daar blijft "Dat onderwerp deelt de laagste score met vijf andere onderwerpen" staan.
- **Meervoud aandachtspunten (taak 3, codereview):** "Daarnaast is A een aandachtspunt." / "Daarnaast zijn A en B aandachtspunten." Pin: `test_report_exit_kernzin::test_kernzin_claimt_niet_dat_startpunt_het_laagst_scoort`.
- **Gebruiksblok blijft tot taak 5 (taak 4):** het plan liet het gebruiksblok in taak 4 van p.02 verdwijnen en nam één rode test voor lief. Twee andere tests pinnen dezelfde leesroute (`test_report_onboarding_eerlijk::test_de_leesroute_noemt_de_hoofdstukken_zoals_ze_heten`, `test_report_onboarding_degraded_agenda::test_de_leesroute_op_p02_komt_uit_bij_een_echte_vraag`), en een blok dat een taak lang stil wegvalt is precies wat Fail Loud verbiedt. Het gebruiksblok rendert daarom tijdelijk in het `leidraad_html`-slot (zelfde plek: vóór de meetgegevens); taak 5 vervangt die aanroep door de leidraad. Geen enkele test is rood.
- **Vertrekreden-cel in het why-blok (taak 4, codereview):** blok 2 en de kernzin noemen de meest genoemde vertrekreden al met getal. Hoort het startpunt bij die reden (alleen of in een gelijkspel), dan maakt de why-cel alleen de koppeling, zonder getal: "Hoofdreden · Meest genoemd · dit onderwerp hangt samen met de meest genoemde vertrekreden" of "Als vertrekreden genoemd · Even vaak · dit onderwerp hangt samen met een van de meest genoemde vertrekredenen". Staat een andere reden hoger, dan heet de cel "Als vertrekreden genoemd" (niet "Hoofdreden", dat sprak zichzelf tegen) met "26× · van de 45 vertrekkers; Beter aanbod elders is vaker genoemd (30 keer)". De noemer volgt blok 2 (wie een reden gaf, als dat minder is); gelijkspel en "vaker" komen uit `exit_r_top`. Helper: `_hoofdreden_cell`.
- **Spreiding- en verdiepingcel alleen als ze een reden zijn (taak 4, codereview):** de spreidingcel staat er alleen als de rij van het startpunt de spreidingsvlag heeft of de spreiding de volgorde besliste (`decided_by`), en boven `MIN_DISTRIBUTION_N`; zonder signaal stond er "0 van de 45 onder de 5". De verdiepingcel vervalt als de gespreksopener dezelfde toelichting al noemt. Een onbekende optiesleutel geeft een fout, zoals bij de richtingopties.
- **Verwijzing op de Loep Start-agenda (taak 4, codereview):** "Dezelfde opener staat op pagina 2." staat er alleen als de zinnen echt gelijk zijn. p.02 kiest het startpunt uit `top_fkeys` (scoring, risico afgerond op 2 decimalen), de agenda uit `_select_priority_factors` (onafgerond); bij een bijna-gelijkstand kunnen die uiteenlopen. Dat verschil is niet opgelost, alleen afgevangen.
- **Bronregel bij een gedeelde laagste score (taak 4, open punt uit taak 3):** "Gebaseerd op de laagst scorende factor." klopt niet als meer onderwerpen de laagste getoonde score delen. Wordt: "Gebaseerd op de laagste score; die deelt dit onderwerp met {naam}." of "... met {telwoord} andere onderwerpen." Geldt voor exit en retention (`_raster_attribution`, alleen in de generieke tak) en voor Loep Start, dat die regel vast had. De K1-tak (toprij boven een lagere score zonder verklarend signaal) blijft ongewijzigd.
- **Bronregel bij de datagedreven opener (taak 4):** de regel "Gebaseerd op de meest gekozen toelichting van respondenten in de verdieping." vervalt samen met `_short_mgmt_q`; de bronregel zegt nu altijd waarom dit onderwerp het startpunt is. De opener zelf noemt de toelichting al.
- **Paginasplitsing in tests (taak 4):** p.02 krijgt `id="p02"` (nodig voor de CSS van taak 6), dus `_page_two` in `test_report_degraded_page_two` zoekt op `<div class="pb sec"` zonder het sluitende `>`; `test_totaalsignaal_staat_met_profiel_in_de_onderbouwingsrij` kijkt naar `class="sg p02-cijfers"`.
- **Genoteerd, niet veranderd (taak 4):** zonder factorprofiel staat het totaalsignaal nu twee keer op p.02: in de degraded kernzin ("De frictiescore van 5.1/10 wijst op een gemengd vertrekbeeld.") en in de cijfersrij, die in die staat ook rendert. Geen tegenspraak; de kernzin is gepind (`_DEGRADED_SIGNAAL`). Ook de drempel van 10 staat er dan twee keer (respons-oordeel en degraded alinea).
- **Geen leidraad zonder factorprofiel (taak 5):** in de degraded staat rendert de leidraad niet; hij zou sturen naar een startpunt, een verdieping en een volgorde die er niet zijn. De degraded alinea op p.02 zegt wat er wel is, de slotpagina draagt een echte vraag. De vier tests die de degraded en de normale leesroute pinden gaan in lockstep mee en controleren nu dat er geen leidraad en geen `class="pref"` staat (degraded) of dat de leidraad met de juiste regel 3 rendert (met profiel): `test_report_direction_degraded::test_leidraad_belooft_geen_lege_secties` en `::test_leidraad_met_profiel`, `test_report_onboarding_degraded_agenda::test_zonder_profiel_geen_leidraad_maar_wel_een_echte_vraag`, `test_report_onboarding_eerlijk::test_de_leidraad_noemt_bij_loep_start_geen_verdieping`.
- **Loep Start zonder regel 4 (taak 5):** zonder afdelingen, zonder open toelichtingen en zonder werkbeleving heeft regel 4 geen sectie om naar te verwijzen; dan rendert de leidraad niet. De werkbelevingssectie en die gate lezen dezelfde variabele (`_ob_has_sdt`), zodat ze niet uiteen kunnen lopen. In de praktijk heeft elke Loep Start-meting werkbeleving.
- **"Responsbasis" heet overal "meetgegevens" (taak 5):** het plan paste alleen de `wel=`-lijsten aan. Drie terugvalzinnen van de kernzin ("Zie de vertrekcontext en de responsbasis ...") en de degraded agenda-alinea van Loep Start ("de responsbasis op de openingspagina") verwezen ook naar een blok dat zo niet meer heet; die zeggen nu "de meetgegevens". Pins: `test_report_respons_gevolgen::test_verwijzing_krijgt_een_eigen_mededeling_in_plaats_van_een_haakje` en `::test_verwijzende_terugval_krijgt_geen_haakje`.
- **Begeleide bespreking ook uit de Loep Start-agenda (taak 5, H5):** `SECTION_INTROS["gespreksagenda"]` (alleen Loep Start) zei nog "maak je in de begeleide managementbespreking"; wordt "maken jullie in de bespreking zelf". Geen test pinde deze zin.
- **Datastatus-pin bij Loep Start (taak 5):** het plan schreef de assertie `"Niet in dit rapport: werkgeversaanbeveling (eNPS)."`; zonder afdelingen staan die er ook in ("Niet in dit rapport: afdelingen (te weinig antwoorden per afdeling), werkgeversaanbeveling (eNPS)."). De test controleert daarom de regel en het eNPS-deel los, en dat "openen zodra" niet op p.02 en in de meetgegevens van alle drie de scans staat.
- **Meetdatums in de noemer-tests (taak 5):** `_basis` in `test_report_respons_gevolgen` krijgt meetdatums, want "Meetperiode niet vastgelegd" raakte de `"vastgelegd" not in`-asserties van twee tests die over de noemer gaan (`test_responsbasis_zwijgt_bij_voldoende_respons`, `test_responsbasis_toont_de_reden_van_een_te_laag_vastgelegd_aantal`).
- **Leidraad kent ook een vlag voor de verdieping (taak 5):** het plan gaf `_leidraad_block` alleen `has_segments`, `has_quotes` en `has_direction`, waardoor regel 3 bij Vertrek en Behoud altijd "en wat mensen als toelichting kozen" beloofde. Er is een verplichte `has_deepening` bijgekomen; zonder verdieping zegt regel 3 "de laagste stelling en de score van elke stelling", wat de verdiepingspagina in die staat wel toont. Nieuwe test: `test_report_p02_mtvel::test_leidraad_belooft_geen_toelichtingen_in_een_meting_zonder_verdieping`.
- **Elke leidraadvlag hangt aan de gate van de sectie zelf (taak 5, codereview):** `bool(deep_agg)` was nog te grof. Het verdiepingsblok toont pas een verdeling vanaf vijf beantwoorde verdiepingsvragen, en alleen voor het onderwerp zelf; bij een startpunt met twee antwoorden beloofde regel 3 de toelichtingen terwijl die pagina zegt dat het er te weinig zijn. De vlag komt nu uit het aggregaat van het startpunt via `_deepening_shows_distribution`, dezelfde staffel die `_deepening_block` nu ook zelf gebruikt (geen nieuwe drempel, één bron). Regel 4 volgt de werkbelevingsgate (`_heeft_werkbeleving`, dezelfde dimensies als de sectie) nu ook bij Vertrek en Behoud, niet alleen bij Loep Start: zonder afdelingen, toelichtingen en werkbeleving rendert de leidraad in geen van de drie producten. `has_direction` blijft op `bool(direction_agg)`: bij te weinig antwoorden rendert het blok "Wat er moet gebeuren" nog wel, met de eerlijke tellingen. De drie renderers kiezen hun vlaggen via één gedeelde `_leidraad_html`, zodat ze niet uiteen kunnen lopen.
- **Regel 1 van de leidraad is productbewust (taak 5, codereview):** de methodiekpagina van Loep Start heeft geen cel Drempelwaarden (daar staat Checkpoint-logica), dus "de drempels staan op pagina N" was daar onwaar. Loep Start zegt nu "wat Loep uit deze aantallen wel en niet afleidt staat op pagina N". Zet taak 11 een drempeltabel op alle drie de methodiekpagina's, dan mag die tweedeling weg en wijst regel 1 naar `LEIDRAAD_ANKERS["drempels"]`.
- **Omgekeerde meetperiode (taak 5, codereview):** een sluitdatum vóór de startdatum werd letterlijk afgedrukt ("30 maart 2026 tot 9 maart 2026"). `build_report_data` vergelijkt de datums nu vóór het formatteren (via `_kalenderdag`, dezelfde dag die `_datum_nl` afdrukt) en geeft `period_dates_conflict`; de meetgegevens zeggen dan "Meetperiode niet betrouwbaar vastgelegd" met één regel eronder waarom, in plaats van een periode die niet kan bestaan.
- **Reden zonder afdelingstabel komt uit de data (taak 5, codereview):** de renderers gaven vast "te weinig antwoorden per afdeling" mee, ook als de organisatie nooit een afdeling had vastgelegd. `_segment_absent_reason` onderscheidt die twee ("niet vastgelegd bij deze meting" tegenover "te weinig antwoorden per afdeling") en `build_report_data` levert hem als `segment_reason`; een fixture zonder die sleutel krijgt geen verzonnen reden maar de kale opsomming.
- **Verwijzing van de gespreksagenda naar pagina twee (taak 5, codereview):** "Dezelfde opener staat op pagina 2." had het nummer hardcoded en klopte alleen zolang p.02 letterlijk pagina twee is; die twee plekken gebruiken nu `_pref("p02")`, met `id="p02"` als anker. De acht tests die de zin pinden delen daarvoor één constante.
- **H4 is na taak 5 nog niet volledig gesloten:** de spec vraagt paginanummers "overal waar naar een sectie wordt verwezen" (par. 4 blok 5). De leidraad heeft ze, en twee van de drie verwijzingen die de leesronde bij H4 citeerde zijn verdwenen met het blok waarin ze stonden: "achteraan de gespreksagenda" (gebruiksblok, p.02) en "segmentbeeld verderop in dit rapport" (Segmentstatus-kaart, p.03). Vier verwijzingen dragen nog geen nummer: `SECTION_INTROS["verdieping"]` ("Wat er volgens hen moet gebeuren staat bij de gespreksagenda", het derde H4-citaat), `OVERZICHTSPROFIEL_RANGORDE` voor Vertrek/Behoud ("bij de gespreksagenda verderop") en voor Loep Start ("staat verderop vooraan bij de thema's met de meeste aandacht en in de gespreksagenda"), en de `divided`-tak van `_direction_p02_line` ("Zie de gespreksagenda."). De eerste drie zijn vaste copy die de taalronde (taak 13) toch herschrijft, dus daar horen ze; de vierde kan niet zonder contractwijziging, want `_bestuurlijke_read` escapet `direction_line` met `_h()` (die regel draagt tellingen en optieteksten) en een `<a class="pref">` erin zou letterlijk worden afgedrukt. Dat vraagt een aparte parameter naast `direction_line`.
- **WeasyPrint-Docker niet gedraaid (taak 5):** de Docker-daemon reageerde in deze sessie niet (`docker ps` en `docker info` liepen in een timeout), dus de PDF-render met nul warnings is voor deze taak niet uitgevoerd. De structuur is wel getest (elke verwijzing wijst naar precies één bestaand anker, in Vertrek, Behoud, Loep Start en in de degraded staat zonder factorprofiel), en de uitkomst is gepind in `test_report_p02_mtvel::test_de_pdf_vult_de_verwijzingen_met_echte_paginanummers`: die rendert met WeasyPrint, eist nul WARNING/ERROR-records op de `weasyprint`-logger (een verwijzing naar een onbekend anker logt daar `Content discarded: target points to undefined anchor`) en controleert in de tekstlaag dat de gevulde nummers naar de echte pagina van de gespreksagenda en het overzichtsprofiel wijzen. De test slaat via `requires_weasyprint` netjes over waar GTK ontbreekt. Het pad `target-counter(attr(href), page)` -> `attr(href)` als string -> anker uit het element-`id` is nagelezen in de WeasyPrint-broncode van het venv (`css/tokens.py::get_target`, `css/targets.py::lookup_target`, `css/__init__.py` dat `-weasy-anchor` uit `id` zet). Taak 6 en 14 moeten de echte render alsnog doen.
- **Genoteerd, niet veranderd:** bij een respons onder `RESPONSE_INDICATIVE_RATE` staat er twee keer een dubbele punt na elkaar ("Indicatief beeld: Behoud vraagt aandacht op één kwetsbaar onderwerp: ..."). Dat was al zo en blijft voorlopig staan.
- **Pagina twee kon niet op een echte PDF gemeten worden (taak 6):** WeasyPrint kan lokaal niet renderen (geen GTK) en de Docker-engine reageerde ook in deze sessie niet (`docker info` liep in een timeout, en de Windows-service start niet zonder beheerdersrechten). De vijf PDF's uit stap 4 van het plan zijn dus niet gerenderd en niet gemeten. Wat wél is gedaan: de structurele eisen zijn in HTML getest voor alle zeven staten van p.02 (drie producten, met en zonder factorprofiel, plus de retentievarianten) en de meetcode van `scripts/check_pdf_report.py` is getest op PDF's die PyMuPDF zelf bouwt met bekende paginavulling en bekende teksten, met per regel een document dat hem overtreedt en een document dat hem haalt. De uitkomst op het echte rapport is gepind in `test_report_p02_mtvel::test_de_echte_pdf_zet_de_meetgegevens_op_pagina_twee`, dat via `requires_weasyprint` netjes overslaat waar de renderer ontbreekt. **Nooit tegen een echte PDF gelopen:** of p.02 met de compacte maten daadwerkelijk op één A4 past, of hoofdstuk 02 in de PDF echt op pagina drie begint, en of de tabelkopregel klopt. Scenario 11 en 20 zijn wel gegenereerd (HTML): p.02 is daar één paginasectie zonder tweede paginabreuk, met de meetgegevens erin, en de sectie erna opent met hoofdstuk 02. Taak 14 moet de render alsnog doen, samen met de openstaande render uit taak 5.
- **Structuur van p.02 was na taak 5 al goed (taak 6):** de losse kaarten Populatie, Segmentstatus en Datastatus zijn in taak 5 in de meetgegevens opgegaan, dus stap 2 van taak 6 vroeg geen productiewijziging meer. De eisen zijn alleen vastgelegd: één `pb sec` voor heel p.02, geen tweede paginabreuk erin, geen losse kaart na de meetgegevens, en de sectie direct erna opent met de hoofdstukkop 02 (in alle drie de producten, ook zonder factorprofiel).
- **De compacte maten van p.02 staan achteraan in het stylesheet (taak 6):** het plan zette ze na `.br-kernzin`. Daar staan ze vóór `.why { ... }`, en twee bestaande designguards (`test_report_html_design::test_why_block_not_navy_background` en `::test_why_block_has_border_left`) lezen met `re.search(r'\.why\s*\{')` de eerste treffer in het stylesheet: die werd `#p02 .why`, een regel zonder achtergrond en zonder left-border, waardoor beide guards omvielen. De overrides staan daarom onderaan, ná elke basisregel die ze aanpassen (wat voor overrides ook de gewone plek is). `test_css_houdt_pagina_twee_compact` pint die volgorde, zodat een volgende verplaatsing de designguards niet opnieuw stilzwijgend op de verkeerde regel laat kijken. Geen test is aangepast.
- **`scripts/check_pdf_report.py` draagt een regelnaam per bevinding (taak 6):** het plan gaf één lijst met foutregels. Taak 6 mag de vullingsregel nog niet halen (die is van taak 8), dus elke bevinding is nu een `Bevinding(regel, melding)` en `check()` neemt een `regels`-selectie; de CLI kan die met `--regel` beperken en noemt de gemeten regels in de slotregel. Verder drie afwijkingen van de schets, alle in dezelfde richting (niets stil laten gebeuren): een document met minder dan drie pagina's geeft een bevinding in plaats van een `IndexError`; een onleesbaar bestand geeft `NIET GEMETEN` met exitcode 2, onderscheiden van `NIET OK` met exitcode 1; en de paginatekst wordt op witruimte genormaliseerd voordat er naar "pagina N" wordt gezocht, want `target-counter` zet het nummer achter het woord en de tekstlaag mag daar afbreken (zelfde normalisatie als de PDF-test van taak 5). De tekstblokken worden op positie gesorteerd, zodat "de eerste tekst van een pagina" niet van de volgorde in de contentstream afhangt. Import via `pymupdf` in plaats van `fitz`: dezelfde bibliotheek, maar `fitz` waarschuwt bij import dat het verdwijnt.
- **Een lege paginaverwijzing is nu zelf een bevinding (taak 6, codereview):** de regel telde alleen de nummers die er stonden, dus een rapport waarin élke verwijzing leeg renderde (ontbrekend anker, "op pagina .") kreeg "geen overtredingen". Het script meldt nu het aantal verwijzingen zonder nummer, en eist minstens vijf gevulde verwijzingen zodra pagina twee de leidraad draagt (die levert er vijf, `_leidraad_block`/`LEIDRAAD_ANKERS`). Het minimum hangt bewust aan de aanwezigheid van de leidraad en niet vast op vijf: zonder factorprofiel rendert de leidraad niet en hoort p.02 er nul te hebben (taak 5), dus een blind minimum zou daar een fout melden die er niet is. De zelfverwijzing "de meetgegevens op deze pagina;" wordt eerst weggemaskeerd, anders leest die prozaregel als een weggelopen verwijzing. Beide markers (de titel van de leidraad en het label "Meetgegevens") staan met een verwijzing naar `report_html.py` in het script: herschrijft de taalronde ze, dan moeten ze hier mee, anders meldt het script een overloop of een weggevallen anker die er niet is.
- **De CLI weigert een regel waarvoor de gegevens ontbreken (taak 6, codereview):** `--regel tabelkop` zonder `--thead` meette niets en zei toch "OK (gemeten: tabelkop)". Dat is nu een harde fout (exitcode 2), net als het omgekeerde (`--thead` zonder die regel in de selectie). Staat de tabelkopregel alleen in de standaardselectie, dan blijft hij optioneel en zegt de slotregel "tabelkop (niet gemeten, geen --thead)".
- **Nieuwe regel `paginaformaat` (taak 6, codereview minor):** de vulling wordt met de A4-marges gerekend, dus op een ander paginaformaat is de uitkomst niet te vertrouwen. Dat is nu een bevinding in plaats van een stille misrekening, en hij wordt altijd gemeten, net als "dit document is te kort om te meten".
- **PyMuPDF staat in `requirements-dev.txt` (taak 6, codereview):** het pakket stond in geen enkel requirements-bestand, terwijl het script en de nieuwe tests het op moduleniveau importeren; zonder het pakket viel de hele collectie van `tests/` om en daarmee de faalset-diff zelf. Naast de regel in `requirements-dev.txt` staat er nu een `requires_pymupdf` in `tests/conftest.py` (zelfde patroon als `requires_weasyprint`) en importeert de testmodule het script geguard, zodat alleen de metende tests overslaan. Geverifieerd met een geblokkeerde import: 65 geslaagd, 20 overgeslagen, collectie heel.
- **De meetcode is ook op een echte WeasyPrint-PDF gelopen (taak 6, codereview):** niet op een verse render (geen GTK, geen Docker), maar op de drie gecommitte voorbeeldrapporten in `docs/examples/`, die nog de render van vóór taak 5 zijn. Uitkomst: alle drie melden "pagina 2 bevat de meetgegevens niet" en "pagina 3 begint niet met hoofdstuk 02 maar met 'Populatie'" respectievelijk "'Segmentstatus'", plus paginavulling van 4%, 15% en 18% op pagina drie. Dat is precies H16 zoals de leesronde hem beschreef, en het bewijst dat de meting op echte output werkt. Wat het niet bewijst: dat de render ná taak 5 en 6 de regel haalt. De vullingscijfers van diezelfde meting zetten de MIN_FILL-grens in perspectief: de laagste pagina die slaagt haalt 42%, de hoogste die valt 38%. Aandachtspunt voor taak 8, vastgelegd bij de constante.
- **Detectie van een lege paginaverwijzing eist de vórm van een verwijzing (taak 6, re-review):** het patroon vlagde elk woord "pagina" vóór een punt, en dat staat ook in gewone copy: "de meetgegevens op deze pagina;", "onderaan deze pagina.", en zonder factorprofiel "de behoudscontext op de volgende pagina". Gemeten: dat gaf één valse bevinding op een correct degraded Behoud-rapport. De maskering van "deze pagina" dekte dat maar half en is vervangen door een patroon dat de twee vormen eist waarin elke echte verwijzing staat, "(pagina X)" en "op pagina X" (`_leidraad_block`/`_pref`). Gemeten over de zeven p.02-staten: met gevulde ankers nul bevindingen, met weggegooide ankers precies zes in de drie staten met leidraad en nul in de vier zonder. De twee markers waarop het script p.02 herkent (de leidraadtitel en "Meetgegevens") zijn nu aan de renderer gepind, zodat de taalronde een rode test krijgt in plaats van een regel die stil niets meer meet.
- **Het minimum telt verwijzingen, niet unieke paginanummers (taak 6, re-review):** twee regels mogen naar hetzelfde hoofdstuk wijzen. Zet taak 11 de drempeltabel op de methodiekpagina, waar regel 1 van de leidraad al naar wijst, dan zou een telling op unieke nummers onder de vijf zakken en een weggevallen anker melden dat er niet is.
- **De restgroepzin verandert in elk rapport met een restgroep (taak 7):** het plan verwachtte dat `_POOLED_ZIN` in `test_report_segment_startpunt` en de twee losse asserties met diezelfde zin ongewijzigd konden blijven, omdat de samenstelling "alleen met `members` rendert". Die drie tests bouwen hun rijen echter met `_department_segment_rows`, en die zet `members` nu altijd. Drie asserties zijn dus in lockstep meegegaan naar de nieuwe copy (de spec schrijft die voor, H20). Zonder noemer per lid rendert de zin alleen het aantal ingevulde vragenlijsten, geen deelsom.
- **Eén gate voor de aangewezen afdeling (taak 7):** het plan gaf `_segment_startpunt` de gates van staat 3 als tweede kopie mee. Een tweede kopie kan stil afwijken van het navy blok, precies wat deze taak moet voorkomen, dus `_segment_start_note` leest nu diezelfde functie: is de uitkomst `None`, dan volgt een van de twee redenen (verschil te klein of afdeling te klein), anders staat 3 met de waarden uit die functie. Het voorbehoud over niet-beoordeelbare thema's blijft uit `factor_rows` komen, want dat zit niet in de vijf velden die de brugzin nodig heeft.
- **Eén startpunt bij Loep Start (taak 7, punt 1 van de taakopdracht):** het commentaar en de spec zeiden dat pagina twee (`top_fkeys`) en de gespreksagenda (`_select_priority_factors`) bij een bijna-gelijkstand uiteen kunnen lopen. Dat is onwaar: beide sorteren oplopend op dezelfde op twee decimalen afgeronde factorgemiddelden, met dezelfde sleutelvolgorde, dus met echte data komen ze altijd op hetzelfde onderwerp uit. Omdat de cover voortaan "Waar het gesprek begint" zegt, mag dat verschil ook niet meer uit een inconsistente fixture kunnen komen: cover, pagina twee, de factordiepte en de gespreksagenda lezen nu één `_ob_startpunt_fk` (uit `_select_priority_factors`, de agenda-logica is niet veranderd). De gelijkheidstest achter "Dezelfde opener staat op pagina 2" blijft staan als guard. Bijkomend: de tak van pagina twee zonder startpunt valt nu samen met `_geen_profiel`, waar eerst een fixture met scores maar zonder `top_fkeys` een why-blok zonder cellen kon renderen.
- **Vertrekredentelling van het startpunt uit de volledige teller (taak 7, punt 2):** `_hoofdreden_cell` kreeg zijn `er_n` uit `exit_r_dist`, de top 5 van de tabel. Zijn meer dan vijf redenen even vaak genoemd en valt die van het startpunt buiten die top 5, dan kwam daar 0 uit en verdween de cel, terwijl ze "Even vaak" moest melden. `build_report_data` levert nu `exit_r_counts` (de hele Counter) en `_exit_reason_count` leest die; een fixture van vóór die sleutel valt terug op de top 5 plus het volledige gelijkspel (`exit_r_top`), zonder een getal te verzinnen.
- **Genoteerd, niet veranderd (taak 7):** het navy afdelingsblok zet achter het laagste thema van de aangewezen afdeling een voorbehoud zodra daar thema's onder de per-factor-gate vielen ("1 thema is daar niet beoordeelbaar"). De brugzin op pagina twee en op de gespreksagenda draagt dat voorbehoud niet; de spec schrijft daar een vaste zin voor. Beide noemen hetzelfde thema met hetzelfde getal, dus er is geen tegenspraak, maar de lezer van pagina twee ziet de nuance pas bij de afdelingstabel.
- **Variant 3 van de brugzin is een toevoeging (taak 7, codereview):** de spec schreef twee varianten voor (ander onderwerp, hetzelfde onderwerp) en "geen brugzin zonder aangewezen afdeling". Er is een derde staat die daar niet onder valt: de afdeling wordt wél aangewezen, maar geen enkel thema daar haalt de per-factor-gate, dus er is geen onderwerp om aan te knopen. Die variant staat nu in par. 5 beschreven, met dezelfde kwalificatie en dezelfde restgroepmelding als het navy blok.
- **Eén bron voor de restgroepkwalificatie (taak 7, codereview):** `_pooled_lager` bepaalt of de gepoolde restgroep onder de aangewezen afdeling uitkomt, en zowel het navy blok als de brugzin leest die. Twee kopieën van diezelfde vergelijking zouden stil kunnen afwijken, en dan spreekt pagina twee de afdelingstabel tegen.
- **Restgroepnoemer uit de campagnelijst, niet uit de respondenten (taak 7, codereview defect 3):** `members` kwam uit `_department_grouping`, dat alleen afdelingen kent waar iemand antwoordde. Een afdeling die wel is uitgenodigd maar waar niemand invulde viel daardoor buiten de noemer: "samen 8 uitgenodigd, 6 ingevuld" (75%) waar het 6 van de 14 is (43%). `_enrich_segment_rows_with_invited` bouwt de ledenlijst nu op uit de respondentkant plus elk label uit de campagnelijst zonder eigen rij, inclusief de afdelingen met nul respons; die staan dus ook in de samenstelling in de tabel. Ontbreekt van één lid de noemer, dan vervalt het percentage en staat de reden in de zin: "6 ingevuld, hoeveel mensen hier zijn uitgenodigd is niet volledig vastgelegd". De respondentkant wordt bij de eerste aanroep vastgelegd (`members_resp`), zodat een tweede aanroep niet op zijn eigen uitkomst verder rekent. Lockstep: `_POOLED_ZIN` en twee asserties in `test_report_segment_startpunt` dragen die reden nu (die rijen gaan niet door de verrijking).
- **Geen tweede bron voor het tweede punt bij Loep Start (taak 7, codereview minor):** de gespreksagenda las het tweede aandachtspunt uit `sorted_f[1]` en de terugval van het primaire thema uit `low_lbl`, naast het startpunt uit `_ob_priority_fkeys`. Bij Loep Start leveren die dezelfde volgorde, maar het waren twee bronnen; beide lezen nu dezelfde rangorde. `low_f`/`low_lbl`/`high_lbl` zijn daarmee verdwenen uit die renderer.
- **De marge van de brugzin op een eigen sectie staat in het stylesheet (taak 7, codereview minor):** `.mq-brug-sec` in `report_css.py` in plaats van een inline `style="margin-top:10px;"` op twee plaatsen; een test pint dat er geen inline style meer op die alinea staat.
- **Genoteerd, niet veranderd (taak 7, codereview):** de plaats van de brugzin op pagina twee (onder de gespreksopener, binnen het why-blok) blijft zoals hij is, en de brugzin draagt nog steeds niet het `omitted`-voorbehoud dat het navy blok bij het afdelingsthema zet. In de variant zonder thema is pagina twee nu wél expliciet ("te weinig antwoorden per onderwerp"), waar het navy blok in diezelfde staat alleen de themakolom laat spreken.
- **Werkbeleving en eNPS renderen alleen met data, en dat kost geen hoofdstuknummer (taak 8):** Vertrek en Behoud rendeerden de werkbelevingssectie onvoorwaardelijk, dus met een lege kaart zodra `sdt_avgs` leeg was, terwijl de leidraad die staat al kende (`_heeft_werkbeleving`) en er niet naar verwees. Sectie en leidraad volgen nu dezelfde gate in alle drie de producten; `ch.opener` wordt pas aangeroepen als er inhoud is, zodat de hoofdstuknummering geen gat krijgt. Lockstep: `test_report_leesbaarheid::test_sectie_intros_aanwezig` bouwde op een fixture zonder SDT-data en kreeg die er nu bij, hetzelfde patroon dat dat bestand al gebruikt voor de open toelichtingen en de appendix. De eNPS-pagina is helemaal verdwenen: dat blok staat nu met zijn tellingen bij de context (H13), en de appendixregel noemt de score of zegt dat hij niet gemeten is (C7, "beschikbaar, zie hoofdrapport" en "in deze wave" zijn weg).
- **Geen PDF-meting mogelijk in deze sessie (taak 8):** WeasyPrint kan lokaal niet renderen (`libgobject-2.0-0` ontbreekt) en de Docker-engine antwoordt niet (`docker info` loopt in een timeout). De vijf PDF's van stap 4 zijn dus niet gerenderd en `scripts/check_pdf_report.py` is niet op een verse render gelopen. In plaats daarvan is de paginavulling gemeten met een Chromium-proxy: de HTML van scenario 06, 11, 18 en 20 in een browser met `body { width: 172mm }` (de tekstkolom van een A4 met de marges van `@page`), per topsectie de hoogte in mm, en daarna de paginering nagebootst (alleen `.pb` breekt af, een `.sec.flow` schuift als geheel door). Die proxy gebruikt Chromium-fontmetriek, niet die van WeasyPrint; reken op enkele procenten afwijking. Uitkomst: **elke pagina die een sectie opent, haalt 48% of meer** (06: 85/54/73/67/54/52/90/100/77/100/68/100/59; 11: 91/85/54/71/68/56/48/90/…/94/…/59; 20: 95/74/61/96/55/72/59/58/…/49). De drie sub-40%-pagina's die overblijven zijn **doorlooppagina's** van secties die zelf een volle pagina vulden: de appendixstaart (18% bij 06 en 11, 6% bij 20), de staart van de afdelingstabel (1% bij 06, 34% bij 11) en bij scenario 06 een staart van 3% onder pagina twee (die pagina komt daar op 103%, een signaal voor de p.02-regel van taak 6, niet voor deze taak).
- **Tweekolomsappendix: wel minder pagina's bij de werkbeleving, niet bij de appendix (taak 8):** gemeten op scenario 11 met dezelfde proxy. Werkbeleving 287mm in één kolom (twee pagina's: 100% en 11%) tegen **233mm in twee kolommen (één pagina, 90%)**: dat is een echte B9-winst en haalt een halflege pagina weg. De appendix gaat van 360mm naar **303mm**, maar blijft daarmee boven de 259mm van één pagina; de staart wordt daardoor dunner in plaats van dat hij verdwijnt (39% wordt 17%). De fallback die stap 4 voorschrijft (`.app-tbl` naar 8.5px en `td { padding: 3px 8px }`) brengt de appendix naar 282mm: nog steeds 23mm over, dus de staart blijft onder de 40% (ongeveer 9%). Die maatregel is daarom **niet doorgevoerd**: hij haalt de regel niet en kost leesbaarheid in de enige tabel die de lezer gebruikt om een score te controleren, terwijl de leesbaarheidsronde van 2026-07-09 juist kleine tekst heeft vergroot. De SDT-tabel mee in de kolommen nemen maakt het erger (327mm): op halve breedte lopen die vraagteksten over twee regels, dus daar is de volle breedte efficiënter. Die tabel blijft dus onder de kolommen staan, zoals het plan schrijft.
- **De vullingsregel is niet haalbaar voor een doorlooppagina (taak 8, open punt):** `MIN_FILL` blijft op 0,40 en er is geen nieuwe drempel bijgekomen, zoals de taakopdracht vraagt. De marge is wel te krap voor wat de regel meet: zodra een sectie langer is dan één pagina, bepaalt de hoeveelheid data hoe vol de staart is, en bij een appendix van zes onderwerpen of een afdelingstabel van tien afdelingen is die staart per definitie dun. Geen CSS-maat lost dat op; de twee uitwegen liggen buiten deze taak en vragen een keuze: (a) `scripts/check_pdf_report.py` zondert een doorlooppagina uit waarvan de vórige pagina door dezelfde sectie volledig gevuld werd (een regelwijziging, en de regel meet dan minder), of (b) de appendix krijgt minder inhoud per pagina, bijvoorbeeld door de kolom "Beeld" (een mini-balk die het getal ernaast herhaalt) te laten vallen, wat de vraagtekst breder maakt en de tabellen korter. Taak 14 moet dit op een echte render beslissen.
- **De tweekolomsappendix leunt op celsplitsing van de renderer (taak 8):** de rij van `.tcol.app-cols` is hoger dan een pagina (303mm), dus WeasyPrint moet binnen die rij afbreken. Dat kan hij: `layout/table.py` geeft per cel een eigen `resume_at` (nagelezen in versie 70 in het venv, `requirements.txt` staat op `weasyprint>=60.0`). Kon hij het niet, dan zou de onderkant van een kolom buiten de pagina vallen en zouden stellingen stil uit het rapport verdwijnen. Die eis is gepind in `test_report_paginavulling::test_pdf_appendix_verliest_geen_rijen_bij_de_kolomsplitsing`, dat met zes onderwerpen van vier stellingen plus twaalf werkbelevingsitems rendert en elke stellingtekst in de tekstlaag terugzoekt; de test slaat via `requires_weasyprint`/`requires_pymupdf` netjes over waar de renderer ontbreekt. Twee kolommen via `.tcol` (display: table) en niet via `column-count` of flex met `gap`: WeasyPrint kent geen `gap` op flex en verdeelt een multicol-blok niet over paginagrenzen.
- **De flow-klasse zit in drie bijna identieke helpers (taak 8):** `_factor_detail`, `_ret_factor_detail` en `_ob_factor_detail` zijn lokale functies in drie renderers; tijdens het bouwen kreeg eerst alleen Vertrek de klasse `sec flow` en bleven Behoud en Loep Start stil op een geforceerde paginabreuk staan, met groene tests. Daarom pint `test_report_paginavulling::test_elke_renderer_laat_de_volgende_verdieping_doorstromen` het gedrag voor alle drie de producten en niet alleen voor het product waar het plan de test schreef. In de praktijk levert de doorstroom zelden een pagina winst: elke verdiepingssectie is in de gemeten scenario's 48% tot 68% van een pagina, dus twee passen er niet samen op. Zij verhinderen wel dat een korte verdiepingssectie (een onderwerp met drie stellingen en zonder verdiepingsdata) een eigen bijna lege pagina opent.
- **Correctie op de proxymeting: de tekstkolom is 178mm, niet 172mm (taak 8, codereview).** `@page` heeft `margin: 18mm 16mm 20mm 16mm`, dus de kolom is 210 &minus; 16 &minus; 16 = 178mm breed en 297 &minus; 18 &minus; 20 = 259mm hoog. De eerste meetronde zette de proxy op 172mm; smaller betekent meer regelafbrekingen, dus hogere secties en een te gunstig vullingscijfer. Alle cijfers uit de eerste ronde zijn daarmee ongeveer 3% optimistisch; de cijfers hieronder zijn op 178mm gemeten en vervangen ze.
- **Appendix terug naar &eacute;&eacute;n kolom (taak 8, codereview besluit 1).** De tweekolomsopmaak loste de B9-klacht niet op en maakte de regel die B9 meet juist slechter: de appendix ging van 360mm naar 303mm, dus bleef boven de 259mm van &eacute;&eacute;n vel, er ging geen pagina af en de staartpagina zakte van 39% naar 17% vulling. Daar kwam bij dat een tabelrij hoger dan een pagina alleen goed gaat als de renderer binnen die rij afbreekt; WeasyPrint 70 kan dat (`layout/table.py` geeft per cel een eigen `resume_at`), maar geen draaiende test dekt dat pad, en zou het misgaan dan vielen stellingen stil buiten de pagina. Eén kolom, opnieuw gemeten op 178mm: appendix 362mm bij Behoud (scenario 06 en 11), staartpagina **40%**, en 337mm bij Loep Start met een staart van 30%. Wat van de gedeelde helper blijft: de refactor zelf (drie keer 45 regels werden &eacute;&eacute;n `_appendix_section`), de kolomkop "Stelling" in plaats van "Vraag", en de eerlijke eNPS-regel. De SDT-tabel blijft als laatste tabel onder de onderwerpen staan. Gepind in `test_report_paginavulling::test_appendix_staat_in_een_kolom`.
- **Werkbeleving in twee kolommen alleen als er iets te halveren is (taak 8, codereview besluit 2).** Bij een volle SDT-set (vier stellingen per dimensie, Vertrek en Behoud) is de winst gemeten: 287mm in &eacute;&eacute;n kolom (twee pagina's) tegen 229mm in twee kolommen, dus &eacute;&eacute;n pagina op 88%. Loep Start meet drie werkbelevingsitems (B1, B5, B9), dus &eacute;&eacute;n stelling per dimensie; twee kolommen halveerden daar een sectie die al dun was (0,32 tot 0,35 op main) en brachten hem op 55%. Eén kolom maakt daar 197mm van, een pagina op **76%**. De keuze hangt aan de data en niet aan het scantype: twee kolommen zodra een kaart meer dan &eacute;&eacute;n stelling draagt, anders alles onder elkaar. Geen nieuw getal: "meer dan &eacute;&eacute;n" is de ondergrens van wat halveren betekent. Krijgt Loep Start later een volle set, dan schuift de opmaak automatisch mee (gepind in `test_werkbeleving_blijft_een_kolom_als_elke_kaart_een_stelling_draagt`).
- **Stand van de vulling na de codereview (taak 8, proxy op 178mm).** Scenario 11 (Behoud, n=180): 89/85/54/70/68/55/48/88/100/100/34/94/100/73/100/40/59. Scenario 06 (Behoud, n=45): 100/0/85/54/72/67/54/50/88/100/77/100/63/100/40/59. Scenario 20 (Loep Start, n=30): 95/74/61/95/76/73/60/57/100/30/47. Elke pagina die een sectie opent haalt 47% of meer. Wat overblijft onder de 40% zijn doorlooppagina's: de appendixstaart (40%, 40%, 30%) en bij scenario 11 de staart van de afdelingstabel (34%; bij scenario 06 past die tabel nu op &eacute;&eacute;n pagina). De appendixstaart ligt daarmee **exact op de grens**: dat is de krapte die de taakopdracht gevraagd had te noteren. Een onderwerp meer of minder in de vragenlijst kantelt hem. De twee uitwegen blijven dezelfde en horen bij taak 14: de regel een doorlooppagina laten uitzonderen waarvan de vorige pagina door dezelfde sectie volledig gevuld werd, of minder inhoud per appendixpagina (bijvoorbeeld de kolom "Beeld", een mini-balk die het getal ernaast herhaalt, laten vallen). `MIN_FILL` blijft 0,40 en er is geen nieuwe drempel bijgekomen. Bij scenario 06 loopt pagina twee nog een haarbreedte over (100% plus een staart die op 0% afrondt); dat is de p.02-regel van taak 6, ook nog niet op een echte render gecontroleerd.
- **Eén gate voor de werkgeversaanbeveling, en fail loud bij ontbrekende tellingen (taak 8, codereview besluit 3).** Drie plekken schreven hun eigen versie van dezelfde voorwaarde (contextblok, appendixregel, de `wel=`-opsomming en de meetgegevens). `_enps_cijfers(data)` is nu de enige bron: hij geeft de score en de tellingen, of twee keer `None` zodra `enps_available` de drempel niet haalt, ook als er een berekende score in de data staat. Een score met ontbrekende `enps_detail` is een datafout en geeft een `ValueError` in plaats van stil "niet gerapporteerd", anders zou een gemeten score onzichtbaar uit het rapport vallen. De appendixregel zegt bij een ontbrekende score **"niet gerapporteerd in dit rapport"** in plaats van "niet gemeten in deze meting": onder de rapportagedrempel is de vraag wel gesteld, en de meetgegevens op pagina twee zeggen in diezelfde staat ook alleen "Niet in dit rapport".
- **Kleinere punten uit dezelfde codereview (taak 8):** `_werkbeleving_section` leest de dimensies uit `SDT_LABELS`, dezelfde lijst als `_heeft_werkbeleving`, in plaats van een hardcoded drietal (een nieuwe dimensie haalde anders wel de gate en stond niet in de sectie, en dan wijst de leidraad naar een pagina die hem niet toont). De dode declaratie `break-before: auto` op `.sec.flow` is weg: die klasse draagt geen `.pb`, dus er stond niets terug te zetten. `.item-tbl td` krijgt `overflow-wrap: break-word`, zoals `.app-tbl td` al had, zodat een lange stelling in een halve kolom afbreekt in plaats van de kolom uit te duwen.
- **Aandachtspunt voor taak 14: het eNPS-blok kan verweesd raken (taak 8).** `.enps-inline` staat als laatste blok op de contextpagina met `no-break`, dus het blok zelf valt niet uiteen, maar het kan als geheel naar de volgende pagina schuiven en daar alleen komen te staan. In de gemeten scenario's gebeurt dat niet (de contextpagina's zitten op 85% en 61% inclusief dit blok), maar bij een langere contextpagina wel. Op een echte render te zien; dan is de keuze het blok aan de sectie vast te houden of het bewust een eigen plek te geven.
- **De Anders-staffel telt de teksten, niet de keuzes (taak 9).** Het plan gaf `_anders_block` de staffel op `other_n` (het aantal mensen dat "Anders" koos). Bij de verdieping mag "Anders" zonder toelichting: `schemas.py` eist een `other_text` alleen bij de richtingvraag (`choice` op `*_other`), niet bij `primary`. `other_n` kan dus hoger zijn dan het aantal teksten, en dan zou de staffel op zes keuzes vier toelichtingen laten zien, onder de ondergrens van `MIN_QUOTES_N` die `_should_show_quotes` voor de open toelichtingen hanteert. De gate telt daarom de niet-lege teksten; dat is strenger dan het plan, nooit ruimer. Schreef niemand iets, dan rendert het blok helemaal niet (de telling staat al in de verdelingstabel erboven), en naast het aandeel staat een absolute vloer `OTHER_MIN_N` van 2: op de minimumbasis van dit rapport haalt één respondent het aandeel al (1 van de 3 is 33%) en van één mens is geen conclusie over de optieset te trekken (codereview taak 9, bevinding 1). Om dezelfde reden staat het blok bij de richtingvraag binnen de tak die de verdeling toont: in de staat `too_few` (onder `DIRECTION_MIN_N`) noemt de kaart geen enkele telling, en daar hoort ook geen aantal "Anders" bij te komen.
- **De kop belooft geen teksten die er niet zijn (taak 9, Fail Loud).** Schreef niet iedereen die "Anders" koos een toelichting, dan wordt "6 van de 12 kozen 'Anders' en schreven een eigen toelichting" onwaar. In dat geval staat er "6 van de 12 kozen 'Anders'; 4 schreven een toelichting". De vervoeging komt uit het bestaande `_tel` en uit `_werkwoord`, dat uit `_tel` is gelicht voor de zinnen waarin het getal niet direct voor het werkwoord staat ("6 van de 12 kozen"): één regel voor de vervoeging, twee vormen (codereview taak 9, bevinding 2). Om dezelfde reden zegt het blok het wanneer het op `MAX_QUOTES` afkapt ("Getoond: de eerste 12 van 15 in ontvangstvolgorde, geen inhoudelijke selectie", dezelfde formulering als bij de open toelichtingen) in plaats van de rest stil weg te laten zoals het plan voorschreef.
- **De toelichtingen worden twee keer geanonimiseerd, en dat is bewust (taak 9).** `backend/main.py` haalt `other_text` bij het opslaan al door `anonymize_text`, dus nieuwe data is bij aankomst schoon. `build_report_data` doet het nog een keer over de aggregaten, omdat rijen van voor die sanitizer (of uit een ander schrijfpad) anders ongefilterd in een klant-PDF belanden; de patronen zijn idempotent, want "[NAAM]" en "[EMAIL]" matchen zichzelf niet. `tests/test_report_tellingen.py::test_build_report_data_anonimiseert_de_anders_teksten` schrijft de rijen rechtstreeks in de database, dus die test valt om zodra de lus uit de rapportlaag verdwijnt (geverifieerd door de lus tijdelijk uit te zetten).
- **Identieke toelichtingen worden niet ontdubbeld (taak 9, genoteerd).** De open toelichtingen gaan door `dict.fromkeys` en een `seen`-set; de Anders-lijst niet. Twee mensen die hetzelfde korte antwoord schrijven zijn twee mensen, en het aantal boven de lijst hoort bij wat eronder staat. Wel valt het op in het stresstest-harnas, dat elke Anders-keuze dezelfde placeholdertekst geeft: de lijst is daar negen keer dezelfde regel. Dat is de fixture, niet de renderer.
- **Opmaak van het Anders-blok staat in het stylesheet (codereview taak 9, bevinding 3).** `.anders-kop`, `.anders-note`, `.anders-list` en `.anders-anon` in `report_css.py` in plaats van inline styles, met `overflow-wrap: break-word` zoals `.item-tbl td` en `.cmc`: een toelichting mag 200 tekens zijn en staat in de richtingkaarten in een halve kolom, dus een lange reeks zonder spatie moet afbreken in plaats van de kaart over de marge te duwen. Alle regels op 10px, want de leesbaarheidsronde van 2026-07-09 heeft juist de eerlijkheidsregels naar 10px getild. Gepind in `test_anders_block_gebruikt_klassen_geen_inline_styles`.
- **Kleinere punten uit dezelfde codereview (taak 9):** de verbatimlijst draagt het anonimiseringslabel van de open toelichtingen ("Automatisch geanonimiseerd: herkende namen en contactgegevens verwijderd", één keer onder de lijst in plaats van per regel, uit de gedeelde constante `ANON_NOTE`); "Anders" staat tussen enkele aanhalingstekens, zoals de richtingkaart de optieteksten al citeert; de beperkte-basis-regel staat vóór de Anders-lijst, want die hoort bij de verdeling erboven; twee tests pinnen de aanroepplekken in `_deepening_block` en `_direction_card_cell`, die anders konden verdwijnen zonder dat iets rood werd; `test_anders_drempel_is_benoemd` is vervallen, want de grensgevallen (19% tegen 20%) dekken dat gedrag al.
- **De toelichting bij een tweede keuze wordt niet verzameld (taak 9).** `secondary_counts` kan ook een `*_other` bevatten, maar `aggregate_deepening` bewaart alleen de tekst bij `primary`: de secondaire toelichting komt nergens in het rapport terug (de "daarnaast genoemd"-samenvatting is in juli verwijderd), en respondentinput die nergens terugkomt hoort niet in een aggregaat. Verzamelen we die later wel, dan hoort hij zichtbaar te worden of niet te bestaan.
- **Positionele koppelbaarheid tussen de tekstlijsten (taak 9, restrisico).** `open_texts` en de Anders-lijsten staan alle drie in ontvangstvolgorde, dus de n-de open toelichting en de n-de Anders-toelichting kunnen van dezelfde respondent zijn. Alle drie zijn geanonimiseerd en gestaffeld op vijf, maar een lezer die de lijsten naast elkaar legt kan twee fragmenten aan één persoon toeschrijven. Niet opgelost (ontdubbelen of husselen raakt de open toelichtingen mee en valt buiten deze taak), wel genoteerd als bekend restrisico.
- **Voor taak 11 (drempeltabel):** de tabel op de methodiekpagina moet de 5 ook noemen als de drempel waaronder deze Anders-lijsten alleen een aantal tonen, met dezelfde woorden als het blok zelf gebruikt ("De teksten tonen we pas vanaf 5, om herleidbaarheid te voorkomen"), plus het aandeel van 20% en de vloer van 2 waarboven het blok verschijnt.
- **De ketenclausules dragen geen percentage (taak 10).** `_telling` zet vanaf `MIN_DISTRIBUTION_N` een percentage achter de telling, maar de noemer-ketens gebruiken alleen de vorm "X van de Y". Vier percentages achter elkaar in één zin zijn ruis, en het percentage hoort bij de vergelijking van groepen: die staat in de bronregel en de verdelingstabel. Dat is dus de vaste vorm met en zonder percentage, niet twee vormen.
- **Drie ketens telden in bereikbare staten niet op (taak 10, Fail Loud).** (1) Een onderwerp waarop de cap elke verdieping verdrong (`offered == 0`, `triggered > 0`; bestaande testcase `test_campaign_gate_active_block_renders_for_capped_factor`) rendeerde met de vorm uit het plan "0 van de 0 beantwoordden die": geen Nederlands en geen telling. Nu: "5 van de 39 respondenten scoorden hier laag; niemand kreeg de verdiepende vraag (zij zaten allemaal al aan het maximum van drie verdiepingen)." (2) `offered > triggered` wordt door `aggregate_deepening` bewust getolereerd (historische triggerregels); de zin uit het plan zou dan zeggen dat `triggered` de vraag kreeg terwijl dat getal kleiner is dan het aantal dat hem echt kreeg. Nu staat er wat waar is: "(17 = wie de vraag kreeg; met de drempel van nu scoren 9 van hen hier laag)". (3) `answered + skipped < offered` kan alleen uit beschadigde data komen (de aggregaties zetten elke entry op answered of skipped); `_statusrest` meldt dat verschil nu ("van 4 antwoorden is niet vastgelegd of de vraag is beantwoord") in plaats van mensen te laten verdwijnen. Bij de richtingketen geldt hetzelfde voor `lowest_n > 0` met `offered == 0` (rijen van vóór de richtingvraag): "niemand van hen kreeg de vraag", waar de oude zin stil bij de opener eindigde. Pin die daardoor verschoof: `test_chain_offered_zero_ends_at_opener` heet nu `test_chain_offered_zero_zegt_dat_niemand_de_vraag_kreeg`.
- **De totaalregel sluit of zegt waarom niet (taak 10, H19).** De som van `lowest_n` over de onderwerpen is het aantal respondenten, behalve bij een respondent die geen enkele stelling over deze onderwerpen invulde: `compute_direction_factor` geeft daar `None`, en dan is "de overige 12" onwaar. In die staat vervalt het woord "overige" en volgt de reden: "Bij 4 respondenten kon Loep geen laagste onderwerp vaststellen: zij vulden geen van de stellingen over deze onderwerpen in." De omgekeerde afwijking (som hoger dan het aantal respondenten) kan met echte data niet bestaan en is dus een codebug of een inconsistente fixture: die gaat naar het log en levert dezelfde zin zonder "overige", geen klantcopy over een telling die niet kan.
- **De noemer van "beantwoordden" is het aanbod, niet wie het als laagste had (taak 10).** Het plan koos `lowest` als noemer zodra er geen aparte aanbodstap staat. Ligt `offered` hoger dan `lowest_n` (`aggregate_direction` logt dat als signaal), dan gaf dat "10 van de 9 beantwoordden de vraag". Die noemer is nu altijd `offered`; bij `offered == lowest_n`, het normale geval, is dat hetzelfde getal.
- **"1 van de 1" is de vorm zonder inhoud (taak 10).** Bij één aangeboden verdieping of richtingvraag leverde de vaste vorm "1 van de 1 beantwoordde die" op, en dat komt in echte data voor (stresstest 12 en 18). Die ene clausule wordt "die ene beantwoordde hem". Pins: `test_deepening_chain_all_singular`, `test_deepening_chain_triggered_plural_rest_singular`, `test_chain_singular_lowest`, `test_chain_singular_gap_offered_and_answered`.
- **Geen twee haakjes achter elkaar in de bronregel (taak 10).** Het plan schreef `Volgens {_telling(top_n, n)} ({n} = de mensen bij wie ...)`, wat met percentage "Volgens 19 van de 36 (53%) (36 = de mensen ...)" werd. De uitleg van de noemer staat nu als bijzin: "Volgens 19 van de 36 (53%); die 36 zijn de mensen bij wie groeiperspectief het laagst scoorde en de vraag beantwoordden."
- **Noemer op de eerste telling van de split_none-bronregel (taak 10, uitbreiding).** Het plan noemde alleen de takken met een letterlijke `{top_n} van de {n}`. De split_none-tak zette twee kale tellingen naast elkaar ("14 kozen A; 14 kozen B"), precies wat `_direction_p02_line` in zijn eigen commentaar al als misleidend beschrijft. De eerste telling draagt nu de noemer, zoals de plurality-tak dat al deed; de tweede hangt eraan.
- **Het maximum aantal verdiepingen komt uit `DEEPENING_CAP` (taak 10).** "het maximum van drie verdiepingen" staat niet als los woord in de copy maar wordt uit de constante afgeleid (via `_TELWOORD`), zodat de klantzin niet stil kan afwijken van de vragenlijst.
- **`_telling` weigert een lege noemer (taak 10).** `y <= 0` geeft een `ValueError` in plaats van "3 van de 0": een telling zonder noemer is precies wat B14 verbiedt. Alle aanroepers zitten achter een staffel (minimaal 3 of 5), dus dit is een guard tegen een toekomstige aanroep, geen bereikbare staat.
- **Eén optelling voor de drie totalen (taak 10).** `_direction_totals` is de enige plek waar aangeboden, beantwoord en overgeslagen over alle onderwerpen worden opgeteld; de totaalregel op de gespreksagenda en het degraded blok lezen die. De wording blijft verschillen ("kregen 45 de vraag, 44 beantwoordden hem" tegenover "kregen 8 deze vraag; 7 beantwoordden die"): het degraded blok heeft geen agenda om de tellingen aan te hangen en zijn zin is in vier tests gepind. Gelijktrekken hoort bij de taalronde (taak 13); de twee staten staan nooit samen in één rapport.
- **`skipped` wordt direct geïndexeerd (taak 10).** `_deepening_chain` leest `agg["skipped"]`, net als `_direction_chain`: de aggregaties zetten die sleutel altijd. Dat legde één incomplete fixture bloot (`_deep_agg` in `test_report_p02_mtvel`), die nu sluit. Met een `.get(..., 0)` zou die fixture stil een keten met twee statusloze antwoorden hebben gerenderd.
- **Antecedent in het Anders-blok (taak 10, klein punt uit de taakopdracht).** "6 van de 12 kozen 'Anders'; 4 schreven een toelichting" miste het antecedent: vier van wie? Wordt "; 4 van hen schreven een toelichting". Pins: `test_anders_block_belooft_geen_teksten_die_er_niet_zijn`, `test_anders_block_enkelvoud_bij_een_toelichting`. De kop van dat blok houdt bewust de vorm zonder percentage (het aandeel staat in de verdelingstabel erboven).
- **De verdelingstabellen dragen nu de volle vorm (taak 10).** Zowel de richting- als de verdiepingstabel toont "27 van de 62 (44%)" in plaats van "44% (27)" naast een kaal aantal onder de staffel. Dat is wat het plan vraagt; het kost breedte in de smalle scorekolom van de richtingkaarten. Op een echte render nog niet gemeten (zie hieronder).
- **Het noemerlabel klopte in vier van de vijf kaartstaten niet (codereview taak 10).** `n` is `agg["answered"]`, maar alleen de clear-tak zei dat; `none_needed`, `plurality`, `split_none` en `divided` noemden datzelfde getal "bij wie dit het laagst scoorde", en dat is `lowest_n`, groter zodra iemand overslaat. Gemeten over de scenario's: 29 foute labels in 15 van de 19, met in scenario 11 "27 van de 62 ... bij wie groeiperspectief het laagst scoorde" drie regels boven "76 van de 180 respondenten hadden dit als eigen laagste onderwerp; 62 van de 76 beantwoordden". Eén bron voor die bijzin: `_dir_n_wie(label)` ("bij wie X het laagst scoorde en die de vraag beantwoordden") voor de kaarten en `DIR_N_DIE_WIE` ("die dit het laagst scoorden en de vraag beantwoordden") voor de vormen achter "de {n}". `_direction_p02_line` gebruikt dezelfde bijzin én dezelfde `_telling`, zodat pagina twee en de gespreksagenda hetzelfde getal met hetzelfde percentage tonen (gemeten: "27 van de 62 (44%)" op beide plaatsen). De clear-regel op p.02 werd "Wat er moet gebeuren volgens X van de Y ...: opdracht": met de bijzin erin stond het werkwoord anders twaalf woorden van zijn onderwerp. Pins: `test_p02_line_per_state`, `test_p02_line_for_the_new_states`, `test_p02_split_none_line_is_singular_correct`, `test_half_none_p02_line_makes_no_majority_claim`, `test_direction_p02_line_draagt_de_noemer_ter_plekke`, plus de vijf kaart-bronregels in `test_direction_report_block`.
- **De totaalregel sloot alleen als `offered == lowest_n` voor elk onderwerp (codereview taak 10).** Zin 1 telt `offered`, zin 2 telt `lowest_n`. Bij een campagne die over de deploy heen liep (`lowest_n > offered`, door `aggregate_direction` zelf gedocumenteerd) stond er "kregen 28 de vraag" naast "de overige 11 een ander onderwerp ... daarom niet uitgewerkt" over elf mensen die de vraag nooit kregen. `niet_gevraagd` wordt nu per onderwerp gemeten (zodat een gat op het ene onderwerp niet wordt weggepoetst door een ander), "overige" vervalt, en er volgt "Bij 11 van de 39 is die vraag niet gesteld; van hen is er dus geen antwoord" (28 + 11 = 39, de zin sluit weer). De zin "Die antwoorden gaan over onderwerpen die niet op de agenda staan en zijn daarom niet uitgewerkt" rendert alleen nog als er buiten de agenda echt antwoorden zijn; anders zou hij een keuze suggereren die Loep niet had. Beide gemengde gevallen zijn getest: `offered = 0` voor een niet-agenda-onderwerp en `offered < lowest_n` voor een agenda-onderwerp.
- **Twee onmogelijke tellingen stoppen de generatie (codereview taak 10; twee van de drie in taak 11 teruggedraaid, zie hieronder).** `toegewezen > n_total` en `offered > toegewezen` (en in `_direction_chain` `offered > lowest_n`) kunnen met echte data niet bestaan: elke respondent draagt aan ten hoogste één onderwerp bij en de servervalidatie staat alleen de eigen laagste factor toe. Ze leverden wel klantcopy op die zichzelf tegensprak ("10 van de 10 beantwoordden de vraag" onder "9 hadden dit als laagste"), dus ze geven nu een `ValueError` in plaats van een warning plus onzin. Drie testfixtures waren intern inconsistent (som van `lowest_n` hoger dan het meegegeven `n_total`) en zijn rechtgetrokken: `test_report_priority_render` (`n_total` 13 -> 17, twee plaatsen) en drie aanroepen in `test_direction_report_block`.
- **Kleinere punten uit dezelfde codereview (taak 10).** `.dir-tbl .is` krijgt `width: 34%` en `white-space: nowrap`: de volle tellingsvorm is ongeveer 91px en auto-layout gaf die kolom in een halve kaart ongeveer 59px, dus hij brak af halverwege het getal (op een echte render nog niet gezien, dat hoort bij taak 14). `.dir-chain.dir-totals` krijgt de kleur van `.dir-intro` (#374151), want het is gewone leestekst. `_telling` verloor zijn ongebruikte `y_is`-parameter: die deed niets met zijn waarde en suggereerde in de test dat hij in de output landde; de uitleg van de noemer hoort in de zin. En "9 van hen" in de tak `offered > triggered` werd "9 respondenten": `triggered` is over alle respondenten geteld, niet over de groep die de vraag kreeg.
- **Blijft staan tot taak 14 (codereview taak 10).** `lowest_n` staat twee keer in het blok (in de totaalregel en in de keten onder elke kaart), en de totaalregel is lang. Beide zijn een leesbaarheidsafweging op een echte render, niet een onwaarheid.
- **Geen PDF-meting mogelijk in deze sessie (taak 10).** De Docker-engine antwoordt niet (`docker info` loopt in een timeout) en WeasyPrint kan lokaal niet renderen (geen GTK), dus de nul-warnings-render is niet uitgevoerd. Wat wél is gedaan: alle 21 stresstest-scenario's zijn opnieuw gegenereerd en elke gerenderde keten is mechanisch nagerekend (57 richtingketens en totaalregels, 49 verdiepingsketens, nul afwijkingen: aanbod is beantwoord plus overgeslagen, en de onderwerpopsomming telt op tot het aantal respondenten). De nieuwe CSS gebruikt alleen eigenschappen die WeasyPrint kent (geen `var()`, geen `gap`, geen `inset`; `ch` staat al in `.dir-intro`). De breedte van de scorekolom en de plaats van de totaalregel op de pagina horen bij de render van taak 14.
- **De drempeltabel staat op alle drie de methodiekpagina's, en daarmee vervalt de tweedeling in de leidraad (taak 11).** `_drempeltabel(scan_type, direction_active=...)` vervangt de cel Drempelwaarden ("5+ responses indicatief &middot; 10+ voor patroonduiding &middot; 5+ per groep voor segmentweergave"), die vier getallen zonder uitleg in jargon noemde en alleen bij Vertrek en Behoud stond. Elke rij heeft de plek waar de drempel werkt en één zin waarom, en elk getal komt uit de constante die hem ook echt stuurt (`MIN_AGGREGATE_N`, `MIN_SEGMENT_N`, `MIN_QUOTES_N`, `OTHER_MIN_N`, `DEEPENING_MIN_N`, `DIRECTION_MIN_N`). Een drempel die in dit rapport nergens werkt krijgt geen rij: Loep Start (geen verdieping, geen richtingvraag) houdt drie rijen, en `direction_active=False` laat de richtingrij weg. Twee rijen dragen bewust hetzelfde getal (`MIN_SEGMENT_N` en `MIN_QUOTES_N` staan beide op 5): het zijn losse constanten die uiteen kunnen lopen en ze gelden op een andere plek, dus is de sortering alleen op het getal, zodat de invoegvolgorde blijft staan. Omdat de tabel er nu in alle drie de producten staat, wijst regel 1 van de leidraad overal naar `LEIDRAAD_ANKERS["drempels"]` en is de tweedeling uit taak 5 ("wat Loep uit deze aantallen wel en niet afleidt") vervallen, zoals die afwijking zelf al voorzag. Lockstep: `test_report_p02_mtvel::test_leidraad_verwijst_bij_loep_start_niet_naar_drempels_die_er_niet_zijn` heet nu `::test_leidraad_verwijst_in_alle_drie_de_producten_naar_de_drempeltabel`, en `::test_zonder_afdelingen_toelichtingen_en_werkbeleving_geen_leidraad` verwacht naast `p02` ook de verwijzing van de ranglijst naar `sec-drempels`.
- **De tabel dekt ook de Anders-drempels, in de woorden van het blok zelf (taak 11, opdracht uit taak 9).** De 5 waaronder alleen een aantal wordt getoond staat er met dezelfde zin als het blok gebruikt, uit één constante `ANDERS_TEKST_DREMPEL` ("De teksten tonen we pas vanaf 5, om herleidbaarheid te voorkomen"); twee kopieën van die zin kunnen stil uiteen lopen, en dan legt de tabel iets anders uit dan het blok doet. Het aandeel van 20% (`OTHER_SHARE_MIN`) en de vloer van 2 (`OTHER_MIN_N`) staan samen in één rij: ze werken als één gate voor hetzelfde blok, dus twee rijen zouden suggereren dat je met één van de twee al iets ziet. Het getal in de kolom "Vanaf" is daar de vloer, want dat is de drempel die als aantal telt; het percentage staat in de uitleg. Die rij staat alleen bij Vertrek en Behoud (Loep Start heeft geen verdieping en dus geen 'Anders'), maar de regel over de teksten geldt ook daar, want de open toelichtingen hebben dezelfde staffel. Eén test die per ongeluk te breed was ging in lockstep mee: `test_report_respons_gevolgen::test_render_zonder_noemer_noemt_nergens_een_responspercentage` zocht elk `\d+%` in het hele rapport en viel over de 20% in deze uitleg. De sweep laat nu alleen de drempeltabel buiten beschouwing (met de eis dat die er ook echt uit is geknipt); elke plek waar over respons wordt gesproken blijft erin.
- **Eén beperkte-basis-regel voor twee plekken (taak 11).** `_beperkte_basis_note()` vervangt twee formuleringen met dezelfde bedoeling: "Beperkte antwoordbasis" onder de verdiepingsverdeling en "Beperkte basis" onder een richtingkaart. Beide dragen nu de verwijzing naar de drempeltabel, zodat de drempel erachter niet meer nergens staat uitgelegd. Bijkomend: de gate van het verdiepingsblok las `answered <= 9` hardgecodeerd en leest nu `answered < MIN_AGGREGATE_N`, dezelfde grens (10) uit de constante die hem stuurt. Lockstep: `test_deepening_report_html::test_block_counts_only_5_to_9`, `test_report_tellingen::test_verdiepingsblok_zet_de_beperkte_basis_regel_voor_de_lijst`, `test_direction_report_block::test_percentages_from_10_and_caveat_at_3_4`, en in `test_report_direction_degraded` twee tests die de oude richtingcel pinden.
- **Versiedrift in de richtingketen degradeert zichtbaar in plaats van hard te falen (taak 11, correctie op taak 10).** Taak 10 liet `offered > lowest_n` (`_direction_chain`) en `offered > toegewezen` (`_direction_totals_line`) een `ValueError` geven, terwijl `aggregate_direction` in zijn eigen commentaar vastlegt dat die staat bewust getolereerd en gelogd wordt: `offered` komt uit de opgeslagen antwoorden en `lowest_n` wordt bij elke render opnieuw uit de ruwe scores berekend, dus een gewijzigde rekenregel voor het laagste onderwerp kan de twee in een historisch rapport uiteen laten lopen. De twee lagen zeiden daarmee iets anders, en de rapportlaag zei het strengst: een harde fout betekent een mislukte klantdownload van een rapport dat verder in orde is. Beide raises zijn vervangen door een eerlijke, zichtbaar gedegradeerde regel (Fail Loud trap 2, dezelfde vorm als `_deepening_chain` bij `offered > triggered` en als `_statusrest`): de keten opent dan vanaf het aanbod ("10 van de 13 respondenten kregen de vraag over dit onderwerp (10 = wie de vraag kreeg; met de rekenregels van nu is dit bij 9 respondenten het laagste onderwerp)") en de totaalregel zegt dat de twee tellingen niet op elkaar sluiten en waarom, zonder te beweren dat die mensen geen stelling invulden. De noemer van "beantwoordden" blijft het aanbod, zodat er nooit "10 van de 9" staat. `toegewezen > n_total` blijft wél een `ValueError`: dat is rekenkundig onmogelijk (elke respondent draagt aan ten hoogste één onderwerp bij), dus een codebug of een inconsistente aanroep, geen drift. Het commentaar in `aggregate_direction` en in beide rapportfuncties zegt nu hetzelfde. Lockstep: `test_report_tellingen::test_keten_faalt_als_er_meer_is_aangeboden_dan_laagst_scoorde` heet nu `::test_keten_degradeert_zichtbaar_als_er_meer_is_aangeboden_dan_laagst_scoorde`, met twee nieuwe tests voor de tak met `lowest_n == 0` en voor de totaalregel.
- **De sorteerregel onder de ranglijst wordt één slotzin (taak 11).** `raster_uitleg` somde de drempels uit ("Spreiding tonen we vanaf 10 responses; verdiepingsduiding vanaf 8 beantwoorders per factor; de vraag om verandering vanaf 3 beantwoorders per factor.") en legde daarmee op de rasterpagina uit wat de drempeltabel nu uitlegt. De regel noemt de drempels nu kort ("De drempels (spreiding vanaf 10, verdieping vanaf 8, richting vanaf 3) staan uitgelegd in de drempeltabel") en eindigt bewust zonder punt: `_prioriteringsraster` zet de paginaverwijzing (`_pref`) en de punt erachter. De functie blijft dus een kale string, want de contract-test pint de volledige string als substring van de HTML en een anker erin zou daar letterlijk in belanden. Lockstep: vier tests in `test_report_priority_render` die de oude opsomming pinden, inclusief de monkeypatch-test op `DEEPENING_MIN_N`.
- **De gelijkspelmelding staat als volle zin onder de tabel (taak 11, C13).** "vrijwel gelijk aan Werkdruk en herstelruimte" stond in de agendakolom van 14%, waar het over vier regels brak en als een label bij de agendarol las. De meldingen staan nu onder de tabel, in rijvolgorde, met het onderwerp als onderwerp van de zin ("Leiderschap staat vrijwel gelijk aan Werkdruk en herstelruimte."). De terugval op de sleutel blijft staan (zoals in de oude cel): `near_tie_with` hoort naar een rij in dezelfde lijst te wijzen, maar een ontbrekende rij mag geen `StopIteration` in een klantrapport geven.
- **De afdelingstabel krijgt kolomkoppen, en beide tabellen dragen ze in een `thead` (taak 11, C9 en ronde 2 observatie 4).** De segmenttabel had geen koppen, dus benoemde de sectie-intro de kolommen in proza; de ranglijstkop stond in een `tbody` en herhaalde daarom niet op een vervolgpagina. Beide staan nu in een `thead` met `display: table-header-group` (`.raster-tbl thead`, `.item-tbl thead`), en de ranglijstkop heet "Onderwerp" in plaats van "Factor": dat is de term die de rest van het rapport gebruikt. Boven de afdelingstabel staat één regel met de twee drempels die daar gelden en de verwijzing naar de tabel.
- **De herhaalde tabelkop is niet op een echte render bewezen (taak 11).** Stap 5 van het plan (scenario 06 met een kunstmatig lege pagina vóór de tabel, renderen en `scripts/check_pdf_report.py --thead` erop) kon niet lopen: WeasyPrint kan lokaal niet renderen (`libgobject-2.0-0` ontbreekt) en de Docker-engine antwoordt niet (`docker version` loopt in een timeout, zoals in de taken 5, 6, 8 en 10). Wat wél is gedaan: in de WeasyPrint van het venv is nagelezen dat precies deze display-waarde een groep tot `is_header` maakt (`formatting_structure/build.py`) en dat zo'n groep buiten de paginabreuk-lus blijft en per paginafragment opnieuw wordt gelegd (`layout/table.py`, `body_groups_layout`). De eis staat daarom als guard op de declaratie die het gedrag waarmaakt (`test_report_drempels_en_koppen::test_css_laat_de_tabelkoppen_herhalen_op_een_vervolgpagina`), naast de structuurtests op de `thead` zelf. Taak 14 moet de meting alsnog doen, samen met de openstaande renders van taak 5, 6, 8 en 10.
- **Elke rij van de drempeltabel hangt aan de sectie waarin hij werkt (codereview taak 11, punt 1).** De tabel stond na de eerste ronde volledig op `scan_type in DIRECTION_SCAN_TYPES` plus `direction_active`, dus in een meting waarin niemand een verdieping triggerde (`deep_agg == {}`, stresstest 04) legde hij twee drempels uit die daar niets doen. `_trust_page` en `_drempeltabel` kregen daarom een `deepening_active`-vlag uit exact dezelfde waarde als de uitlegregel onder de ranglijst (`bool(deep_agg)`), en de renderers van Vertrek en Behoud geven hem mee. **Afwijking van de reviewopdracht:** de Anders-rij en het fragment "en de teksten bij 'Anders'" hangen niet alleen aan `deepening_active` maar aan `deepening_active or direction_active`. Het Anders-blok staat namelijk ook onder een richtingkaart (`_direction_card_cell`), dus in een meting zonder verdiepingsdata maar met richtingantwoorden is die drempel wél in werking, en dan hoort de uitleg er te staan: het principe is "geen rij over een drempel die nergens werkt", niet "geen rij zonder verdieping". Gemeten op de 21 stresstestscenario's: scenario 04 gaat van 7 naar 5 rijen, Loep Start houdt 3, de rest houdt 7.
- **De meest getoonde onderdrukking staat nu in de tabel (codereview taak 11, punt 2).** `_deepening_shows_distribution` hield de 5 als kaal getal in de code, terwijl die staffel in acht van de 21 scenario's de regel "Te weinig verdiepingsantwoorden om een verdeling te tonen" oplevert en in geen enkele uitleg stond. De constante heet nu `DEEPENING_DISTRIBUTION_MIN_N` (zelfde waarde, geen nieuwe drempel), de tabel heeft er een rij voor ("de verdeling van toelichtingen onder een onderwerp") en de melding zelf verwijst naar de tabel met een paginanummer. Bewust niet samengevoegd met `MIN_QUOTES_N` of `MIN_SEGMENT_N`, die ook op 5 staan: drie losse constanten, drie plekken, drie redenen (stabiliteit tegenover herleidbaarheid).
- **De richtingcel verwijst met een paginanummer in plaats van met "hierboven" (codereview taak 11, punt 3).** "De drempel van 3 staat in de drempeltabel hierboven." was een positieclaim die nu al breekt: de methodieksectie loopt over twee pagina's, met de tabel op de eerste en die cel op de tweede. De cel gebruikt daarom `_pref`, zoals elke andere verwijzing. Dat kan niet in de celtekst zelf, want `_cells` haalt titel en tekst door `_h()` en zou het anker letterlijk afdrukken (hetzelfde probleem als bij `direction_line`, zie de afwijking van taak 5 over H4); de cel mag daarom een derde element dragen met al-gerenderde HTML, die alleen daar wordt gebouwd. Lockstep: `test_direction_report_block::test_trust_page_explains_direction_question_for_exit_and_retention_only` en twee tests in `test_report_direction_degraded`.
- **Het overschot van de richtingketen wordt per onderwerp gemeten (codereview taak 11, punt 4).** De eerste ronde rekende `max(0, offered - toegewezen)` op de sommen, terwijl `niet_gevraagd` al per onderwerp ging. De waarschijnlijkste drift laat respondenten tússen onderwerpen schuiven, dus blijft de som gelijk en zag de regel niets: bij growth=(12,4,4,0) en workload=(4,12,12,0) met 16 respondenten stond er "kregen 16 de vraag, 16 beantwoordden hem" en twee zinnen later "Bij 8 van de 16 is die vraag niet gesteld", zonder één woord over de acht die hem op het andere onderwerp wél kregen. Nu `sum(max(0, offered - lowest_n))` over de onderwerpen. Daarbij zijn de twee slotmeldingen ontkoppeld: het overschot hoeft niet in de groep zonder herberekend laagste onderwerp te passen (toegewezen 15 van 16 naast een overschot van 8), dus de vorm "en 8 van hen kregen de vraag toch" kan over één mens gaan. Het zijn nu twee losse, elk ware zinnen, en de reden "zij vulden geen van de stellingen over deze onderwerpen in" staat er alleen zonder drift: met drift kregen die mensen de vraag wél en leest die reden als een tegenspraak. Lockstep: `test_report_tellingen::test_totaalregel_degradeert_bij_meer_aanbod_dan_laagste_onderwerpen` (de gecombineerde zin uit de eerste ronde), plus twee nieuwe tests voor de drift-tussen-onderwerpen en voor het te grote overschot.
- **Kleinere punten uit dezelfde codereview (taak 11).** De driftopener van `_direction_chain` noemde zijn eigen getal twee keer ("(10 = wie de vraag kreeg" drie woorden na de 10) en zegt nu waar het op staat: "(met de rekenregels van nu is dit bij 9 respondenten het laagste onderwerp, dus die twee tellingen sluiten niet op elkaar)". De Anders-rij zegt er nu bij dat het blok wegblijft als niemand van hen een toelichting schreef (het aantal staat dan al in de verdeling erboven), de rij van `DEEPENING_MIN_N` noemt ook de kolom Verdieping van de ranglijst (dezelfde drempel stuurt `_deepening_state` in `report_priority.py`), en het ambigue "daaronder blijft het beeld een gesprekshaakje" in de richtingrij is vervangen door wat er echt gebeurt: "bij drie of vier antwoorden staat er een beperkte-basis-regel bij". De drempelregel boven de afdelingstabel dubbelde de sectie-intro (die de bundeling vanaf vijf en de staffel van 5 tot 9 al uitlegt) en is teruggebracht tot de verwijzing zelf.
- **Naar taak 14 (codereview taak 11).** De methodieksectie loopt sinds de drempeltabel over twee pagina's; of dat de goede knip is, is een vraag voor een echte render. Idem voor de lengte van de gelijkspelzinnen onder de ranglijst bij een vlak profiel (scenario 06 heeft er drie). Twee drempels staan nog niet in de tabel: de appendixgate en de percentagestaffel van `_telling`; de appendixgate (`_should_show_appendix`) hangt aan twee dingen tegelijk (meer dan 20 respondenten en meer dan 5 onderwerpen) en past niet in de vorm van deze tabel, en de percentagestaffel van `_telling` is dezelfde tien als de eerste rij, nu met een andere formulering.
- **Een organisatienaam die zelf op een punt eindigt krijgt er geen tweede (taak 12).** `VERSPREIDINGSREGEL` zet een punt achter de naam, dus "TechBouw B.V." werd "TechBouw B.V.." op de cover en op de slotpagina. `_verspreidingsregel` haalt die dubbele punt eruit; de naam zelf blijft ongemoeid (afkappen op de laatste punt zou "B.V" maken). Geen nieuwe drempel, geen nieuwe copy.
- **De verdiepingsrijen van de drempeltabel hangen ook aan de ranglijst (taak 12, restpunt uit de review van taak 11).** `_drempeltabel` kreeg `deepening_active` in taak 11, maar zonder factorprofiel (stresstest 07) rendert er geen ranglijst en geen verdiepingsblok, terwijl `deep_agg` gevuld kan zijn: de rij van `DEEPENING_MIN_N` beloofde daar "de kolom Verdieping in de ranglijst" en de rij van `DEEPENING_DISTRIBUTION_MIN_N` een verdeling die nergens staat. `_trust_page` had `ranking_active` al (voor de banden-cel) en geeft hem nu door; het verdiepingsblok hangt aan diezelfde vlag, want `priority_fkeys` komt uit de rasterrijen. De Anders-rij volgt de verdiepingsvlag naast de richtingvraag (het blok staat onder beide). Gemeten: scenario 07 gaat van 7 naar 5 rijen (de twee verdiepingsrijen weg; de Anders-rijen blijven, want daar staat de richtingvraag), scenario 11 en 20 blijven gelijk. Nieuwe test: `test_report_drempels_en_koppen::test_drempeltabel_laat_de_verdiepingsrijen_weg_zonder_ranglijst`.
- **De totaalregel claimt bij drift niet meer dat er geen antwoord is (taak 12, restpunt uit de review van taak 11; in dezelfde taak gecorrigeerd, zie hieronder).** `niet_gevraagd` en het overschot overlappen bij versiedrift: van de mensen zonder aanbod op hun eigen laagste onderwerp kan een deel de vraag over een ánder onderwerp hebben gekregen, en voor dat deel was "van hen is er dus geen antwoord" onwaar. De eerste versie koos de formulering op één globale vlag (`overschot > 0`) en gaf de hele groep het label van het verschoven deel.
- **`_overzicht_summary_and_bands` houdt zijn banden (taak 12, C3).** Alleen het gerenderde lijstje verdwijnt; de functie levert de indeling nog, want die is de plek waar de bandgrenzen op de getoonde score worden gerekend en dat is los getest (`test_report_band_rounding::test_overzicht_bands_use_displayed_score`). De drie renderers laten de tweede waarde vallen.
- **C3 maakt de overzichtsprofielpagina dunner (taak 12, naar taak 14).** De bandlijst was in juli toegevoegd omdat die pagina half leeg stond, en de leesronde vraagt hem er nu weer af omdat hij de balken erboven herhaalt. Beide zijn waar: de herhaling is weg, de pagina is korter geworden. Of dat onder de vullingsregel van taak 8 duikt, is niet gemeten: WeasyPrint kan lokaal niet renderen (geen GTK), de Docker-engine antwoordt niet (`docker version` liep opnieuw in een timeout) en `playwright` staat niet in het venv, dus ook de Chromium-proxy van taak 8 was deze sessie niet beschikbaar. Op de echte render van taak 14 te beslissen; de samenvattingszin, de intro en de zes balken blijven de inhoud van die pagina.
- **Het anonimiseringslabel boven de toelichtingen draagt een punt (taak 12, klein).** De plantekst schrijft hem zo. Het Anders-blok zet dezelfde constante zonder punt onder zijn lijst; beide regels staan in mono-kapitalen op 8px, zoals ze vóór deze taak ook stonden. Gelijktrekken hoort bij de taalronde (taak 13), niet bij een label dat van plaats verandert.
- **Geen PDF-meting mogelijk in deze sessie (taak 12).** Zelfde situatie als in de taken 5, 6, 8, 10 en 11. De uitkomst is wel op gerenderde HTML gecontroleerd (scenario 07, 11 en 20): de verspreidingsregel staat één keer op de cover en één keer in de cel "Wie dit mag zien" op de slotpagina, het anonimiseringslabel staat één keer boven negen kaarten in plaats van negen keer, en de bandlijst is uit alle drie de rapporten verdwenen.
- **De groep zonder aanbod wordt gesplitst, niet herlabeld (taak 12, spec-review).** De vlag hierboven klopte alleen zolang er twee onderwerpen in het spel waren. Met drie valt de groep uit elkaar: bij groeiperspectief 15/20, werkdruk 8/8 en leiderschap 16/0 op 39 respondenten stond er "Bij 16 van de 39 is de vraag niet over hun laagste onderwerp gesteld", terwijl er 28 aanbiedingen waren; hoogstens 5 (het overschot) kregen een vraag over een ander onderwerp en 11 kregen er helemaal geen. De regel meldt nu alleen `max(0, niet_gevraagd - overschot)`: het deel dat zeker geen vraag kreeg, met de zin die daar waar is. Het verschoven deel krijgt geen tweede, herlabelde melding, want de overschotmelding verantwoordt die mensen al met hun eigen getal ("Bij 5 van de 39 ging de vraag over een onderwerp dat ... niet hun laagste onderwerp is"); een aparte zin met `min(niet_gevraagd, overschot)` zou datzelfde feit twee keer zeggen, met twee verschillende getallen zodra de tellingen uiteenlopen. Zonder drift is `zonder_vraag` gelijk aan `niet_gevraagd` en verandert er niets. Gemeten op de vier staten: gat + overschot (28 + 11 = 39, de zin sluit), alleen gat, alleen overschot, en gat gelijk aan overschot. Lockstep: `test_report_tellingen::test_totaalregel_meet_het_overschot_per_onderwerp_niet_op_de_som` (die pinde eerst de herlabelde staart en verwacht nu geen tweede melding); nieuw: `::test_totaalregel_splitst_wie_geen_vraag_kreeg_van_wie_een_andere_vraag_kreeg` en `::test_totaalregel_zwijgt_over_wie_geen_vraag_kreeg_als_iedereen_er_een_kreeg`, plus een assertie op het zuivere gat in `::test_totaalregel_meldt_wie_de_vraag_niet_kreeg`.
- **De source-guard rendert meer staten dan het plan schetste (taak 13).** De fixtures uit het plan (`_fixture` en `_min_retention_data`) hebben geen verdiepings- en richtingdata, geen open toelichtingen en geen eNPS, dus bleef een groot deel van de klantcopy ongerenderd en bewaakte de guard die woorden niet: de richtingkaarten, het Anders-blok en de richtingrijen van de drempeltabel kwamen er nooit in voor. Er staan nu vier extra staten in de sweep: een volle meting per product (verdieping, richting, toelichtingen, afdelingen, eNPS), dezelfde volle meting met een kwetsbaar startpunt (de enige staat waarin de split_none-kaart rendert, want die eist een score onder de 5,0), en de degraded richtingstaat zonder profiel per onderwerp (stresstest 07). De rijke verdiepings- en richtingdata hangt aan de LAAGST scorende onderwerpen en niet aan de dict-volgorde van de contentset: anders landt ze op onderwerpen die buiten de agenda vallen en toont het rapport juist de dunne staten (gemeten: dat gebeurde in de eerste versie van de guard, die daardoor de verdelingstabel en het Anders-blok niet zag).
- **De werkbelevingsintro noemt geen aantal (taak 13).** Het plan schreef "Naast de zes onderwerpen over het werk". `SECTION_INTROS["werkbeleving"]` wordt door alle drie de renderers gebruikt (via `_werkbeleving_section`), en Loep Start meet landingsdomeinen en niet de zes organisatie-onderwerpen; een hardgecodeerd telwoord zou daar onwaar zijn. Wordt: "Naast de onderwerpen over het werk". De `behoudscontext`-intro en de sigrow houden wel "de zes onderwerpen over het werk": die renderen alleen bij Loep Behoud, waar het er zes zijn (`ORG_FACTOR_KEYS`).
- **De verspreidingsregel noemt de toelichtingen niet meer (taak 13, review ronde 2).** Spec par. 10 (H15) schreef "de toelichtingen zijn geanonimiseerd maar herkenbaar in kleine teams". Die regel staat op de cover en op de slotpagina van ELK rapport, ook in een meting zonder open toelichtingen en zonder verdieping (stresstest 07), en beloofde daar iets over een blok dat er niet is. Wordt: "Deel dit rapport niet met individuele medewerkers; in kleine teams zijn uitkomsten herkenbaar, ook al noemt dit rapport geen namen." Dat is waar in elk rapport en benoemt precies de reden waarom een klein team alsnog herkenbaar is. Bijkomend: daarmee staat de anonimiseringsclaim nog maar een keer op de slotpagina (de cel Open toelichtingen), waar hij eerst ook in de cel Wie dit mag zien stond; gepind in `test_report_taal_guard::test_de_anonimisering_staat_een_keer_op_de_slotpagina`.
- **De cel "Wie dit mag zien" opent met het antwoord op zijn eigen titel (taak 13, review ronde 2).** Hij begon met "Verwerking conform AVG" en zette de verspreidingsregel erachter. Omgedraaid, niet gesplitst: een aparte AVG-cel zou een vierde cel in die rij zijn en zegt op zichzelf niets dat de lezer kan gebruiken.
- **De cel "Claimgrenzen" heet "Wat dit rapport niet doet" (taak 13, bijlage B).** Met de copy uit de plantabel per product, plus twee kleinere jargonfixes in dezelfde rij: bij Loep Start heet "Checkpoint-logica" nu "Een meetmoment", en "Alleen bij voldoende n getoond" is "Alleen getoond vanaf {MIN_QUOTES_N} toelichtingen" geworden (het getal uit de constante, zoals de drempeltabel het ook doet). "Verlooppredicties" en "uitvalpredicties" zijn in dezelfde beweging "voorspellingen over vertrek" en "voorspelling van uitval" geworden: het plan noemde alleen de intro, maar dezelfde woorden stonden in de cellen ernaast.
- **`_drempeltabel` kreeg `direction_degraded` (taak 13, review ronde 2).** `anders_actief` hing aan `direction_active`, maar in de degraded richtingstaat rendert `_direction_degraded_block` alleen tellingen en geen kaarten, dus staat daar geen Anders-blok onder een richtingkaart en werkt die drempel in dat rapport nergens. `_trust_page` had de vlag al en geeft hem nu door. Gemeten op de degraded staat: de Anders-rij en het fragment "en de teksten bij 'Anders'" verdwijnen, de richtingrij blijft (die vraag is wel gesteld en het degraded blok toont haar tellingen). Nieuwe test: `test_report_drempels_en_koppen::test_anders_rij_blijft_weg_in_de_degraded_richtingstaat`.
- **De noemer van een richtingkaart staat achter de telling in plaats van erin (taak 13, review ronde 2).** Drie staten (`none_needed`, `plurality`, `split_none`) zetten de bijzin van tien woorden tussen onderwerp en werkwoord: "27 van de 62 (44%) bij wie groeiperspectief het laagst scoorde en die de vraag beantwoordden kozen die richting". De clear-tak had de goede vorm al; die vorm is nu een gedeelde helper `_dir_noemer_zin`, die alle vier de staten gebruiken, zodat het noemerlabel niet op vijf plaatsen los kan lopen. De noemer blijft `answered` en het label blijft dat van `_dir_n_wie`. **Niet veranderd:** de `divided`-kaart ("De 8 die dit het laagst scoorden en de vraag beantwoordden kozen verschillend.") en de `none_needed`- en `divided`-regels op pagina twee houden hun betrekkelijke bijzin; die is korter en de review noemde alleen de drie kaarten. Gepind in `test_report_taal_guard::test_geen_tangconstructie_tussen_telling_en_werkwoord`, dat de vorm over alle gerenderde rapportstaten sweept.
- **Geen haakje binnen een haakje op pagina twee (taak 13, review ronde 2).** De plurality-regel zette de telling met haar percentage in een tweede paar haakjes: "(27 van de 62 (44%) die dit ..., zonder meerderheid)". Wordt dezelfde vorm als de clear-tak, met de nuance als bijstelling tussen komma's: "Wat er moet gebeuren volgens de grootste groep, 27 van de 62 (44%) die dit het laagst scoorden en de vraag beantwoordden, zonder meerderheid: ...". Gepind met een guard die elke telling met percentage over alle rapportstaten controleert op een open haakje ervoor.
- **"Indicatief beeld." met een punt (taak 13, review ronde 2).** Dit draait de notitie "Genoteerd, niet veranderd" van taak 5 terug: de kernzin draagt zelf al een dubbele punt zodra hij de kwetsbare onderwerpen opsomt, en "Indicatief beeld: Behoud vraagt aandacht op een kwetsbaar onderwerp: werkdruk" zette er twee in een zin.
- **"beide onderwerpen" bij precies twee (taak 13, review ronde 2).** "alle twee onderwerpen scoren 6.2/10" is geen Nederlands. Nieuwe helper `_alle_onderwerpen(n, kaal=)` voor de twee plaatsen in `_p02_flat_sentence`; geen nieuwe drempel, geen nieuw getal. Lockstep: `test_report_p02_profielvorm::test_vlakke_zin_telt_het_echte_aantal_factoren`.
- **`.cdist` breekt lange woorden af (taak 13, review ronde 2).** De verspreidingsregel op de cover draagt een organisatienaam; zonder `overflow-wrap: break-word` duwt een lange naam zonder spatie die regel over de marge. Dezelfde eigenschap die `.app-tbl td`, `.item-tbl td`, `.cmc` en `.anders-list` al hebben, en de enige CSS-wijziging van deze taak (geen `var()`, geen `gap`, geen `inset`).
- **De guard vond nog twee plekken die het plan niet noemde (taak 13).** "Bespreek dit onderwerp in de managementbespreking." onder een te kleine verdiepingsverdeling is "in de bespreking zelf" geworden, dezelfde formulering die H5 al voor de gespreksagenda voorschreef. En het voorbehoud "{n} thema('s) niet beoordeelbaar" op drie plaatsen in de afdelingstabel gebruikt nu `_tel(n, "onderwerp", "onderwerpen")`: "1 onderwerp" tegenover "2 onderwerpen" in plaats van de haakjesvorm.
- **Lockstep taak 13: 22 testbestanden.** Mechanisch op tekstniveau doorgevoerd met een verwacht aantal treffers per paar, zodat een niet-passend fragment de sweep stopt in plaats van hem half door te voeren: `test_direction_report_block`, `test_pdf_redesign`, `test_report_degraded_page_two`, `test_report_degraded_verwijzingen`, `test_report_design_sprong`, `test_report_direction_degraded`, `test_report_drempels_en_koppen`, `test_report_exit_kernzin`, `test_report_leesbaarheid`, `test_report_onboarding_degraded_agenda`, `test_report_onboarding_eerlijk`, `test_report_p02_mtvel`, `test_report_p02_profielvorm`, `test_report_priority_attribution`, `test_report_priority_consistency`, `test_report_priority_render`, `test_report_respons_gevolgen`, `test_report_segment_startpunt`, `test_report_startpuntverhaal`, `test_report_taal_guard`, `test_segment_factor_themes`, `test_segment_report`. `test_report_generation_smoke` is bewust ongemoeid: die pint de legacy ReportLab-renderer (`backend/report.py`), die buiten deze taalronde valt. Een assertie is inhoudelijk bijgewerkt in plaats van woordelijk: `test_segment_report::test_segmentblok_gerenderd_bij_data` zocht "causale" in het hele document, en die claimgrens heet nu "Geen uitspraken over oorzaken".
- **Geen PDF-meting mogelijk in deze sessie (taak 13).** Zelfde situatie als in de taken 5, 6, 8, 10, 11 en 12: de Docker-engine antwoordt niet (`docker info` en `docker version` lopen in een timeout) en WeasyPrint kan lokaal niet renderen. De enige CSS-wijziging is een eigenschap die het stylesheet op vier andere plaatsen al gebruikt; de rest van deze taak is copy. De uitkomst is wel op gerenderde HTML gecontroleerd: nul treffers van de verboden woorden in de zichtbare tekst van tien rapportstaten, nul em-dashes en nul losse streepjes, en de drie rapporten zijn per sectie doorgelezen.
- **De cel "Wat dit rapport niet doet" wijkt af van de plantabel, en Lars moet deze formulering zien (codereview taak 13).** De plantabel schreef "Geen uitspraken over oorzaken, geen advies over maatregelen". Dat spreekt het rapport zelf tegen: het noemt de meest genoemde vertrekreden en de toelichtingen uit de verdieping (oorzaken, zoals mensen ze zelf aandragen), en het blok "Wat er moet gebeuren" staat vol opdrachtvormen. De grens is waar gemaakt in plaats van weggelaten: Loep Behoud "Loep stelt zelf geen oorzaken vast: de redenen in dit rapport komen van je mensen. Geen kant-en-klaar actieplan: wat er gebeurt, beslist het MT."; Loep Vertrek dezelfde twee zinnen met "Geen oordeel over vermijdbaarheid en geen voorspellingen over vertrek." ertussen; Loep Start "Loep stelt zelf geen oorzaken vast en voorspelt geen uitval. Geen kant-en-klaar actieplan: wat er gebeurt, beslist het MT." (Loep Start heeft geen vertrekredenen of verdieping, dus daar geen zin over "redenen van je mensen"). De source-guard is niet aangepast om dit door te laten. Gepind in `test_report_taal_guard::test_wat_dit_rapport_niet_doet_spreekt_het_rapport_niet_tegen`; lockstep `test_segment_report::test_segmentblok_gerenderd_bij_data`.
- **Werkbelevingsintro na de sweep rechtgezet (codereview taak 13).** "Die onderwerpen alleen" sloeg na de eerste sweep op de drie basisbehoeften, en "een laag onderwerp" bestaat niet. Wordt: "De onderwerpen over het werk alleen vertellen niet het hele verhaal. Een lage score op een onderwerp, met een gezonde werkbeleving, vraagt een ander gesprek dan wanneer beide onder druk staan."
- **Drie mechanische vervangingen hersteld (codereview taak 13).** "Bespreek dit onderwerp in de bespreking zelf." was een tautologie en wordt "Bespreek dit onderwerp in het MT."; de openingsvraag rolhelderheid (Behoud) "... een onderwerp?" wordt "Speelt onduidelijkheid over eigenaarschap, prioriteiten of beslisruimte mee?". De andere `_MGMT_Q`-zinnen zijn nagelopen: dit was de enige die door de sweep was geraakt.
- **Resterend jargon weg (codereview taak 13).** "(SDT)" uit de appendixkoppen ("Werkbeleving: alle stellingen", bij Loep Start "Werkbeleving: stellingen bij het checkpoint"; "B1 t/m B12" was itemcode en ging mee); "responses" wordt "antwoorden" in de afdelingsintro, de spreidingscellen, de aanwijs- en meldregels van de afdelingstabel; "Onderwerpbeeld per afdeling" wordt "Alle onderwerpen per afdeling"; de lege afdelingsstaat zei "Verdieping opent zodra voldoende responses per groep beschikbaar zijn", wat botst met de verdiepingsvragen, en zegt nu voor alle drie de producten "De tabel per afdeling verschijnt zodra er per afdeling genoeg antwoorden zijn." (`SEGMENT_VERVOLG` en `SEGMENT_VERVOLG_ONBOARDING` hebben daarmee dezelfde tekst; beide constanten blijven omdat tests en renderers ze apart noemen). De guard zoekt nu ook `(sdt)`, `responses`, `verdieping opent zodra`, `onderwerpbeeld` en de tautologie. **Follow-up, niet gedaan:** "n=60" in de appendix, "(n=17)" in de subblokken per afdeling en "Frictiescore" (Loep Vertrek, onder andere in de degraded kernzin en gepind in `_DEGRADED_SIGNAAL`) raken meer dan deze plek en blijven voor een volgende ronde.
- **De gespreksopener staat twee keer, als dezelfde zin met een verwijzing (taak 14, vastgelegd).** Spec par. 4 blok 4 schreef dat de agenda naar pagina twee verwijst "in plaats van een tweede te maken". Gebouwd is: de opener staat op pagina twee én op de gespreksagenda als letterlijk dezelfde zin, met op de agenda "Dezelfde opener staat op pagina N." (`_pref("p02")`, zie taak 4 en 5). Reden: de agenda is de pagina waar het gesprek gevoerd wordt, en een kale verwijzing laat de lezer daar terugbladeren. Er ontstaat geen tweede opener, alleen een herhaling van de eerste.
- **De vlakke-profiel-zin noemt alle onderwerpen op de laagste getoonde score (taak 14, vastgelegd).** Uitwerking van spec par. 4 blok 1 en ronde-2-punt (a): de vergelijking gebeurt op de getoonde, afgeronde score, en bij een gedeelde laagste score noemt de zin elk gelijk onderwerp ("laagste score 6.2/10, gedeeld door drie onderwerpen: A, B, C"), zie taak 3. Niet meegegaan: de samenvattingszin van het overzichtsprofiel ("De laagste score zit bij ...") rekent nog op de ruwe score en noemt één onderwerp; in scenario 06 een ander dan het startpunt. Genoteerd in de stresstest (sectie "Na plan 3a", observatie 1), niet opgelost in plan 3a.
- **De restgroep toont haar samenstelling alleen als de datalaag de leden kent (taak 14, vastgelegd).** Spec par. 5 (H20) vraagt de noemer en de namen van de afdelingen in "Overige afdelingen". Die staan er ("HR, Marketing; samen 7 uitgenodigd, 7 ingevuld") zodra `members` gevuld is, uit de respondentkant plus de campagnelijst (taak 7). Een restgroeprij zonder ledenlijst (rijen die niet door `_enrich_segment_rows_with_invited` gaan) rendert zonder namen en zonder deelsom; er wordt geen samenstelling verzonnen.
- **Taak 14 in twee delen (19 september).** De Docker-engine draaide niet, dus stap 3 (WeasyPrint op de drie voorbeelden en op 06, 11, 18) kon niet. Deel A is gedaan: seeds met meetdatums en `invited_count` in `scripts/stresstest_report.py` en `generate_voorbeeldrapport.py`, alle 21 scenario's en de drie voorbeelden als HTML gegenereerd en gelezen, matrix "Na plan 3a" voor Q1, Q2, Q3, Q5 en Q6, guards en faalset. Q4, B9, de herhaalde tabelkop (punt c) en de paginaverwijzingen staan in de matrix als "nog niet gemeten: wacht op WeasyPrint-render". De voorbeeld-HTML's en -PDF's zijn niet gecommit, zodat er geen nieuwe HTML naast oude PDF's staat; ze gaan samen in deel B.
- **Nafix na taak 14 deel A: de samenvattingszin van het overzichtsprofiel noemt nu dezelfde gelijkstand als pagina twee (stresstest "Na plan 3a", observatie 1).** Dit draait het "Niet meegegaan" bij de vlakke-profiel-zin hierboven terug. `_overzicht_summary_and_bands` kreeg een verplicht keyword `laagste` (geen default: een stille terugval op het eerste onderwerp was precies de fout); de drie renderers geven `_p02_laagste_keys(profile_shape(fa))`, dezelfde bron en dezelfde volgorde als de kop. Vorm zoals de kop: "Drie onderwerpen delen de laagste score (6.2/10): A, B, C." (telwoord, dubbele punt, komma's), met "kwetsbare" ertussen als ze kwetsbaar scoren; tonen alle onderwerpen dezelfde score, dan "Alle drie onderwerpen scoren 6.2/10."; zonder gelijkstand "X scoort het laagst." "Geen onderwerp scoort kritisch" is "kwetsbaar" geworden. Eén onderwerp in de kwetsbaar-zonder-sterk-tak heet nu "het enige kwetsbare punt" in plaats van "het duidelijkste" (dezelfde regel als de tak met een sterk onderwerp). Source-guard: `\bkritisch\b`, als los woord, omdat de richtingoptie "Meer ruimte voor kritische vragen" een andere betekenis heeft en moet blijven. Lockstep: `test_report_band_rounding::test_overzicht_bands_use_displayed_score` (alleen de nieuwe parameter).
- **Nafix: Loep Vertrek telt "hoofdreden", en elke telling staat in de vertrekcontext (observatie 3).** De ranglijstkolom, de why-cellen op pagina twee, de verdiepingskaarten, de rasterintro en -uitleg, de markeringsregel (`report_priority`), de bronregel en de kernzin telden alleen de hoofdreden (`exit_reason_code`) maar zeiden "als vertrekreden genoemd" of "meest genoemde vertrekreden", naast een pagina die ook de meespelende redenen toont (voorbeeldrapport: Beloning 18 keer meespelend, 0 in de kolom). Eenvoudigste ware vorm gekozen, geen tweede telling erbij: overal "als hoofdreden (van vertrek) genoemd" en "meest genoemde hoofdreden van vertrek"; het cellabel op pagina twee heet "Hoofdreden van vertrek". Vindbaarheid: de vertrekcontext toonde van beide lijsten alleen de top 3, terwijl de ranglijstkolom en de verdiepingskaarten uit `exit_r_dist` tellen en de why-cel "Speelt ook mee" uit `cont_dist`, beide een top 5. Beide lijsten tonen nu wat `build_report_data` levert (tot vijf), en de kop "(top 3)" is weg. Gemeten met WeasyPrint (Docker weer beschikbaar): de vertrekcontext blijft in het exit-voorbeeld en in scenario 08 één pagina, inclusief het eNPS-blok, 0 warnings. **De weging verandert niet:** `EXIT_REASON_WEIGHT`, `exit_code_counts` en `_select_priority_factors` zijn ongemoeid; een tekstdiff van alle 21 scenario's voor en na laat alleen labelwijzigingen en de extra tabelrijen zien, geen andere volgorde. **Niet gedaan:** de why-cel op pagina twee telt uit de volledige teller (`_exit_reason_count`) en kan dus een reden noemen die buiten de top 5 van de tabel valt, maar alleen als vijf andere redenen even vaak of vaker genoemd zijn; en de kaart "Relatie met het overzichtsprofiel" zegt bij een gelijkspel nog "De meest genoemde hoofdreden" in het enkelvoud (observatie 7a, buiten deze fix). Lockstep, alleen de nieuwe woorden: `test_report_exit_kernzin` (4 regels), `test_report_p02_mtvel` (14), `test_report_priority_attribution` (1), `test_report_priority_direction_tiebreak` (2), `test_report_priority_render` (6), `test_report_respons_gevolgen` (1), `test_report_startpuntverhaal` (1).
- **Nafix: Loep Start noemt een gedeelde laagste niet meer "tweede laagste" (observatie 4).** De why-regel onder het tweede aandachtspunt van de gespreksagenda kijkt nu in `_p02_laagste_keys`: deelt het tweede punt de laagste getoonde score, dan staat er "Deelt de laagste score (5.3/10) met Informatiedichtheid en werktempo." (bij een gelijkstand van drie of meer met de andere namen, met komma's); anders blijft "Tweede laagste score in het overzichtsprofiel." Nieuwe tests voor de drie nafixes: `tests/test_report_plan3a_nafixes.py`.
- **Bijvangst: de guard op "responses" was dood.** In `VERBODEN` stond het patroon met twee backspace-tekens (0x08) in plaats van de woordgrens `\b`, dus het matchte nooit. Hersteld naar `\bresponses\b`; de guard blijft groen, er stond dus ook geen "responses" meer in de klantcopy.
- **Fixronde WeasyPrint punt 1: de overzichtskaart van de werkbeleving staat boven de kolommen (stresstest "Na plan 3a", observatie 8).** Taak 8 zette de balkenkaart in de linkerkolom; die kaart (naam, balk, score en bandlabel op één regel) is breder dan een halve kolom, dus de linkercel groeide en de rechterkolom liep van het vel, afgesneden in 20 van de 21 scenario's en in de voorbeelden Loep Vertrek en Loep Behoud. Nu: de overzichtskaart over de volle breedte, daaronder `.tcol.wb-cols` met `table-layout: fixed`, geen `border-spacing` maar 10px padding per kolom (de kaarten lijnen links uit met de overzichtskaart), de eerste kolom de grootste helft van de kaarten (autonomie en competentie links, verbondenheid rechts), en in de kolommen compactere maten (kaartpadding 10px, celpadding 4px 6px, stelling 70% van de kolom, balk 44px in plaats van 80px). Zonder die compacte maten liep de linkerkolom één kaart over naar een eigen pagina. Gemeten met WeasyPrint-Docker: de sectie is één pagina (06 en 11 84%, 08 95%) en alle 24 renders halen de nieuwe regel `zijmarge` in `scripts/check_pdf_report.py` (geen woord buiten de 16mm-marges; de cover uitgezonderd). Grens van die regel: een woord dat geheel voorbij de paginarand begint, levert de tekstlaag niet op; een kolom die van het vel loopt heeft altijd woorden die de rand overschrijden, en die ziet de regel. Loep Start (één stelling per dimensie) houdt één kolom en de balk van 80px.
- **Fixronde WeasyPrint punt 2: `check_pdf_report.py` meet het meetgegevensblok en de tabelkop hoofdletterongevoelig (observatie 11 en 12).** De marker "Meetgegevens" werd hoofdlettergevoelig gezocht in een tekstlaag die `text-transform: uppercase` in hoofdletters zet, dus de regel `p02-op-een-a4` meldde op elke render een overloop. Hoofdletterongevoelig als substring zou op de leidraadzin "de meetgegevens op deze pagina" altijd slagen. Nu: `MEETGEGEVENS_MARKERS = ("Meetgegevens", "Meetperiode")`, elk als eigen regel van de tekstlaag, hoofdletterongevoelig. Twee markers omdat bij negen van de tien overlopen de blokkop op pagina twee bleef en alleen de rij doorschoof; met alleen de kop was die overloop niet gezien. Op de 24 renders van deze ronde (vóór punt 3) meldt de regel precies de tien overlopen uit observatie 9 en geen andere. `--thead` vergelijkt hoofdletterongevoelig en met genormaliseerde witruimte. De WeasyPrint-test `test_de_pdf_vult_de_verwijzingen_met_echte_paginanummers` gebruikt `_retention_met_secties()` in plaats van `_min_retention_data()`, die sinds taak 5 geen leidraad meer oplevert. Via de Docker-shim (niet in de repo): 11 van de 13 `requires_weasyprint`-tests geslaagd; `test_pdf_heeft_geen_pagina_onder_veertig_procent[exit]` en `[retention]` falen op paginavulling (exit p3 38% en de gespreksopener-vervolgpagina 30%, retention de gespreksopener-vervolgpagina 30%), niet meer op de marker.
- **Fixronde WeasyPrint punt 3: pagina twee past in alle 24 renders, zonder inhoud te schrappen (observatie 9, H16).** Blok 1 tot en met 6 van par. 4 staan ongewijzigd en in dezelfde volgorde op p.02; alleen maten veranderden, allemaal onder `#p02` zodat de rest van het rapport zijn ruimte houdt. Kernzin 24px naar 20px en volle breedte (`max-width: none`); leidraad: beschrijvingskolom van 53% naar 67% (tijdvak 11%, titel 22%), celpadding 2px, zodat de meeste regels op één regel passen; meetgegevensblok via `.meet-blok` in plaats van inline 22px plus 18px witruimte boven de kop; kleinere tussenruimtes in het waarom-blok en de gespreksopener; cijfers 18px, waarom-cijfers 22px. Gemeten met WeasyPrint-Docker: vóór de fix liep p.02 over in 10 van de 24 (tot circa 124pt in 08), nu in 0, met als krapste restruimte 24pt (08), daarna 51pt (09) en 57pt (voorbeeld Loep Vertrek). Structureel: de langste koppen in de stresstest tellen 476 tot 480 tekens (01, 09, 19); op de krapste pagina (08) past een kop van 500 tekens nog op 20px (7,8pt over). Boven `KERNZIN_LANG = 500` tekens zet de renderer de kernzin in `.kz-lang` (18px); gemeten met een kop van 562 tekens op de pagina van 08: past, 19pt over, waar dezelfde kop op 20px circa 21pt tekortkwam. Grens: een kop plus meer why-cellen of langere cijfers dan in 08 is niet gemeten; `check_pdf_report.py --regel p02-op-een-a4` is de meting die dat bij een nieuwe render moet laten zien. Lockstep: de CSS-pins in `test_css_houdt_pagina_twee_compact` (maten, geen klantcopy).
- **Fixronde WeasyPrint punt 4: geen losse segmentconclusie, geen losse gespreksopener (observatie 10, B9).** (a) Het navy blok "Waar het per afdeling begint" staat direct onder de afdelingstabel en vóór de uitsplitsing per afdeling, niet meer onderaan; onderaan paste het in vijftien scenario's niet en stond het alleen op een vel (7 tot 10%). De afdelingstabel en de uitsplitsing zijn compacter (`.seg-tbl`, celpadding 4px; kolom "Laagste onderwerp" van 20% naar 27%, zodat het onderwerp met band in twee regels past). De uitlegregel "Alle onderwerpen per afdeling" reist mee met het eerste subblok; bij twee of meer afdelingen met een uitsplitsing staan de subblokken twee aan twee naast elkaar (`.sub-cols`, elk paar een `tbody.sub-grp` met `break-inside: avoid`), wat scenario 11 van drie vellen met een staart van 17% naar twee maakt. (b) De melding zonder afdelingstabel (Loep Vertrek bij te weinig antwoorden, 07/08/18 en het voorbeeld) houdt kop en melding bij elkaar (`no-break`) en past onder een volle werkbelevingspagina dankzij een compactere werkbeleving (kaarten 8px) en een negatieve `margin-top` (-24px) die de samengevallen witruimte tussen de twee secties van 44px naar 20px brengt; zonder die ingreep stond "06 Per afdeling" als laatste regel onder de werkbeleving en de melding alleen op het volgende vel. (c) Gespreksagenda (Loep Vertrek en Loep Behoud): de invulregels Prioriteit, Eigenaar en Vervolgmoment staan naast elkaar in drie cellen in plaats van onder elkaar in één cel, het navy blok is compacter, en blok plus slotregel "Nog niet besluiten ..." vormen één `no-break`-eenheid (`.agenda-slot`). Daarmee verdwijnt de vervolgpagina van 30 tot 32% in 11 en 13 en de losse slotregel van 15. Loep Start (`_eerste_managementspoor`) is niet aangepast: daar staan de invulregels in de derde kolom naast de onderwerpen en was er geen losse pagina. **Niet opgelost:** scenario 14 houdt een vervolgpagina van 20% met alleen het navy blok: het richtingblok toont daar negen Anders-toelichtingen (de harness vult ze met dezelfde placeholder) en is te lang om het navy blok nog op de pagina te laten.
- **Fixronde 2 punt 1: ook het eerste verdiepingsonderwerp stroomt (B9, observatie 5).** Het eerste onderwerp opende een eigen vel; daardoor bleef het laatste onderwerp alleen op een pagina van 27 tot 37% (01, 03, 04, 09, 18, 19). Nu zijn alle onderwerpen `sec flow verd`: het hoofdstuk begint onder het overzichtsprofiel als het eerste onderwerp daar als geheel past, en anders op een nieuw vel. Tussen twee onderwerpen staat 24px; de negatieve marge op het eerste (`.verd-eerste`, -20px) brengt de witruimte na het overzichtsprofiel ook op 24px. Gevolg voor H4: de verwijzing "verdieping" in de leidraad wijst dan naar een pagina die met het overzichtsprofiel begint, waarop hoofdstuk 04 halverwege opent (zelfde situatie als de segmentanalyse). Binnenmaten bewust niet compacter bij Loep Vertrek en Loep Behoud: gemeten op de WeasyPrint-render trok elke compactere variant (kopmarge 0 tot 14px, celpadding, kaartpadding) in 08, 12, 13 of 15 het tweede onderwerp naar voren, zodat het derde juist alleen kwam te staan (23 tot 41%); de marge boven "Alle stellingen over dit onderwerp" ging van 28px naar 16px, de grootste waarde waarbij in geen enkel scenario een nieuw los onderwerp ontstond (gemeten: 0, 8, 16 en 28px). Loep Start krijgt wel compactere binnenmaten (`.verd-compact`: kop 14px, cellen 5px, kaarten 10px); zonder die maten stond het tweede onderwerp van hoofdstuk 04 alleen op een vel (32 tot 34%). Geen inhoud geschrapt; de variant "onderwerp zonder verdiepingsdata valt weg met één regel" is niet nodig gebleken voor de vulling en dus niet gebouwd (observatie 5 zelf, de belofte "De aantallen hieronder zijn tellingen" in 04, staat dus nog open). **Niet opgelost:** 01 en 19 houden het derde onderwerp alleen op een pagina van 36%: drie onderwerpen van elk ongeveer een derde vel, en het eerste past niet onder het overzichtsprofiel. Dat is bin packing; elke maat die 01 en 19 oploste, brak 08, 12, 13 of 15.
- **Fixronde 2 punt 3: zonder profiel stromen overzichtsprofiel en verdieping mee (stresstest 07).** Onder de tien antwoorden waren beide hoofdstukken één zin op een eigen vel (10 en 12%). Zonder factorscores zijn ze nu `sec flow` (kop en zin blijven samen): in 07 staan ze onder de vertrekcontext (pagina 3 naar 95%) en telt het rapport 6 in plaats van 8 pagina's; `check_pdf_report.py` OK. De tekst is ongewijzigd. Uitzondering: bij Loep Start is het overzichtsprofiel hoofdstuk 02 en volgt het direct op pagina twee; daar blijft het een eigen vel (`stroom_zonder_profiel=False`), anders zou het onder de meetgegevens op p.02 schuiven (H16, gevonden door `test_pagina_drie_begint_met_hoofdstuk_02`). De lege verdieping stroomt in alle drie de producten; 07 heeft geen leidraad, dus geen paginaverwijzing die naar dit hoofdstuk wijst.
- **Fixronde 2 punt 2: de appendix stroomt onder het vorige hoofdstuk (B9).** De appendix is langer dan een vel (voorbeeld Loep Vertrek 96% plus 36%, Loep Start 93% plus 32%), dus met een eigen vel bleef een staart onder de 40%. Nu `sec` zonder paginabreuk: hij breekt alleen tussen de tabellen (elke tabel blijft `no-break`), en kop, intro, n-regel en eerste tabel vormen samen één `no-break`-eenheid. Geen leidraadverwijzing wijst naar de appendix (`LEIDRAAD_ANKERS` heeft geen anker daarvoor), dus een kop halverwege een pagina raakt H4 niet. Lettergrootte en celpadding ongewijzigd (9px). Gemeten: Loep Start (20 en voorbeeld) OK; bijvangst: in 14 vult de appendix nu de pagina met het losse navy blok van de gespreksagenda (was 20%), dus ook 14 OK. **Niet eerlijk op te lossen: de appendix van het voorbeeld Loep Vertrek.** Daar past de eerste tabel niet meer onder de gespreksagenda (70%), dus begint de appendix op een nieuw vel en blijft de werkbelevingstabel (twaalf stellingen plus de eNPS-regel) als staart van 36% over. Cijfers: de appendix is 1,32 vel; om de regel te halen moet hij onder 1,00 vel of boven 1,40 vel uitkomen. Op de ondergrens van 8,5px met 3px celpadding (gemeten via CSS-injectie) wordt het 88% plus 32%, dus 1,20 vel, nog steeds met een staart onder de 40%; in het voorbeeld Loep Behoud maakt diezelfde maat van een goede staart (49%) juist een foute (32%). Twee kolommen voor de werkbelevingstabel is eerder gemeten als langer (codereview taak 8). Voorgelegd aan Lars via de coördinator: mag een appendix-staartpagina een uitzondering op de vullingsregel zijn? Niet zelf ingevoerd; `MIN_FILL` ongewijzigd.
- **Eindreview punt 5: de brugzin zegt niet meer "tweede punt" (par. 5, brugzin).** De spec schreef "neem dat als tweede punt voor die afdeling". Op dezelfde gespreksagenda heet de kaart van de organisatie "Tweede punt: X", dus las de brugzin als een tweede tweede punt, met een ander onderwerp. Wordt: "Bij Operations springt Werkdruk en herstelruimte eruit (4.9/10); bespreek dat voor die afdeling na het startpunt." De bandbewuste neutrale vorm boven de aandachtspuntgrens is ongewijzigd.
- **Eindreview punt 4: eNPS met een echt minteken en één woord voor de groep.** Een negatieve aanbevelingsscore stond als "-91" (koppelteken) terwijl de schaal ernaast "−100 tot +100" schrijft; nu `_enps_str`: "−91" (U+2212) in het blok op de contextpagina en in de appendixregel, positief blijft "+8". De uitleg sprak van "criticasters" en de tellingen van "critici"; overal "critici".
- **Eindreview punt 3: de drempeltabel heeft twee tien-rijen, elk met de constante die zijn gate stuurt (B20/H18).** Eén rij "profiel per onderwerp, spreiding, en een afdeling als startpunt" las `MIN_AGGREGATE_N`, terwijl spreiding, de scores per onderwerp per afdeling en een afdeling als startpunt op `MIN_DISTRIBUTION_N` draaien. Beide zijn nu 10, maar het zijn losse constanten; de tabel splitst ze (net als de drie vijf-rijen) zodat hij niet stil kan afwijken als één van beide verschuift. Percentages staan bewust niet in de rij: het responspercentage en de afdelingsrespons tonen ook onder de tien een percentage, alleen `_telling` volgt die staffel.
- **Eindreview punt 2: één anonimiseringslabel dat precies zegt wat er weggaat.** De methodiekcel "Open toelichtingen" beloofde "herkende namen, contactgegevens en locaties", het label onder de teksten "herkende namen en contactgegevens". `anonymize_text` (`backend/scoring.py`, `_PATTERNS`) vervangt namen (heuristiek: twee woorden met een hoofdletter), e-mailadressen, telefoonnummers en postcodes, en geen plaatsnamen. `ANON_NOTE` zegt nu precies dat ("herkende namen, e-mailadressen, telefoonnummers en postcodes verwijderd") en staat ook in de methodiekcel van alle drie de producten; de intro van de open toelichtingen volgt. Een test koppelt het label aan de vervangtokens in `_PATTERNS`.
- **Eindreview punt 1 (Important): "laag" in de verdiepingsketen is nu "duidelijk laag", met de regel erbij.** De keten telde "wie hier laag scoorde" volgens de triggerregel van de verdiepende vraag, terwijl de spreidingsstrook, de ranglijst en pagina twee "onder de 5" als kwetsbaar tellen; in stresstest 08 stond "Kwetsbaar 7" boven "(3 = wie hier laag scoorde)". Het zijn twee verschillende regels, niet een strengere en een ruimere: de vervolgvraag volgt uit de ruwe antwoorden op de stellingen van dat onderwerp (iemand met 3, 3 en 2 zit onder de 5 zonder vervolgvraag, iemand met 2, 2 en 5 krijgt hem boven de 5). De keten zegt nu "(3 = wie hier duidelijk laag antwoordde)", en de eerste keten die in het rapport rendert sluit af met de regel: "Duidelijk laag: gemiddeld 2,5 of lager op 1 tot 5, minstens twee stellingen op 2 of lager, of een 1 bij gemiddeld hoogstens 3,5. Een andere regel dan ‘onder de 5’." Eén keer en niet achter elke keten, en de hoofdstukintro is compacter ("Wie op dit onderwerp duidelijk laag antwoordde, kreeg automatisch een korte vervolgvraag: welke toelichting past het best bij hun ervaring? De aantallen zijn hun eigen keuzes, geen interpretatie achteraf. Wat er volgens hen moet gebeuren staat bij de gespreksagenda."; weg is "Zo zie je niet alleen dát een onderwerp laag scoort, maar ook wat de groep zelf als reden aandraagt."). Reden: het eerste verdiepingsonderwerp moet onder het overzichtsprofiel passen. Met de regel achter elke keten, of alleen achter de eerste zonder kortere intro, of met de regel in de intro, paste het in stresstest 09 en 18 niet meer en stond het laatste onderwerp alleen op een vel (26 tot 39%, `check_pdf_report.py` NIET OK; op a67f2cd0 waren 09 en 18 OK). In de gekozen vorm zijn alle 21 scenario's OK, ook 01 en 19 die op a67f2cd0 NIET OK waren. De geschrapte zin gebruikte bovendien zelf "laag" in de groepsbetekenis, naast de nieuwe term. De getallen komen uit nieuwe benoemde constanten in `deepening.py` (`TRIGGER_AVG_MAX`, `TRIGGER_WITH_ONE_AVG_MAX`, `TRIGGER_LOW_ITEM_MAX`, `TRIGGER_LOW_ITEM_COUNT`), die `_is_triggered` zelf gebruikt; het gedrag van de trigger is ongewijzigd. Ook de hoofdstukintro ("Respondenten die op dit onderwerp duidelijk laag antwoordden ...") en de ranglijstcel van staat 5 volgen: die zei "score boven de drempel", wat onwaar was bij een onderwerp onder de 5 waar niemand de vraag kreeg, en wordt "niemand antwoordde hier duidelijk laag".

## Afwijkingen bij plan 3b

- **Twee teksten per route (par. 6, bijlage A):** `WORK_QUESTIONS` heeft per routesleutel een tekst voor Behoud en een voor Vertrek, omdat de respondenttekst bij 13 van de 36 routes tussen de scans verschilt. `work_question` gebruikt `scan_type` echt; `direction_imperative` doet dat niet en is dus geen patroon om over te nemen.
- **`plurality` krijgt de route-eigen vraag (par. 6 punt 2):** de spec zette `plurality` bij de verdeelde staten. In die staat toont het rapport al de opdrachtvorm van de grootste route (35% of meer, voorsprong van twee); de verdeeld-zin zou die informatie negeren. Aanname, besluit bij Lars.
- **`split_none` krijgt een eigen zin:** B is daar de niets-optie, en "welke van de twee kunnen jullie waarmaken" is dan onzin (niets doen kan altijd).
- **`divided` zonder twee inhoudelijke routes krijgt geen vertaalvraag:** is `*_other` de grootste veranderoptie, of is er maar één inhoudelijke route gekozen, dan toont het blok alleen de herkennings- en de besluitvraag. Nooit "A en Anders", nooit een verzonnen tweede route.
- **`none_needed` en `too_few` krijgen geen vertaalvraag:** er is geen route om op te kiezen. Bij `none_needed` laat de besluitvraag het MT ook besluiten hier niets te doen (zie Taak 5 van plan 3b).
- **Vertrek heeft een eigen verdeeld-zin in de verleden tijd.** De verdeeld-zinnen (Behoud, Vertrek, `split_none`) zijn gated content, net als de 72 vertaalvragen.
- **De vertaalvragen noemen het onderwerp niet bij naam.** Het blok eromheen levert de naam via `_fl(fk, scan_type)`.
- **Terugval van de herkenningsvraag noemt de score, niet "zo laag" (par. 6 punt 1):** bij een vlak profiel is het startpunt een aandachtspunt of relatief sterk, en dan is "zo laag" onwaar. Wordt: "Wat zit er volgens jullie achter de 6.2/10 op [onderwerp]?" Dezelfde terugval geldt als `*_other` de grootste toelichting is: een Anders-antwoord is geen toelichting om te citeren.
- **Besluitvraag bij `none_needed`:** de richtingkaart zegt daar al dat volgens de meeste betrokkenen niets hoeft. De besluitvraag laat het MT dan ook besluiten hier niets te doen, met eigenaar en een moment om opnieuw te kijken.
- **Het blok rendert ook zonder richtingdata** (meting van vóór de richtingvraag): herkennen en besluiten kan altijd; alleen de vertaalvraag vervalt.
- **De H7-regel staat er alleen bij Vertrek en Behoud met verdiepingsdata.** Bij Loep Start bestaan geen toelichtingen; de zin zou daar iets beloven dat er niet is.
- **Voetregel van de besluitpagina (par. 7):** de spec belooft "bij een vervolgmeting zet Loep dit besluit op pagina twee". De vervolgmeting is plan 3c; tot die er is staat er "Leg dit besluit ook vast in je dashboard. Loep drukt het dan voor in dit rapport en bewaart het bij deze meting." Plan 3c zet de spec-zin terug.
- **Eén eigenaar en één vervolgdatum per besluit (par. 7):** de spec geeft het tweede punt "dezelfde drie regels". De tabel heeft één `owner` en één `follow_up_date`; het tweede punt krijgt daarom alleen "Wat precies". Twee eigenaren vragen een schemawijziging en zijn niet gevraagd.
- **`review_when` blijft als ongebruikte parameter** in `_prioriteringsraster` en `_eerste_managementspoor`: de hint staat nu op de besluitpagina. Opschonen van de aanroepers is een losse refactor.
- **Het werkvragenblok staat binnen `.agenda-slot` (Taak 6, niet in het plan).** Tot plan 3b stroomde de appendix direct achter de gespreksagenda door en vulde die de staart van de agendapagina. De besluitpagina staat daar nu tussen (`pb`), dus die staart wordt niet meer gevuld: in de meting in het productie-image bleef het navy vlak met de gespreksopener in **acht van de 21 scenario's alleen op een vel achter, 11 tot 13% gevuld** (05, 10, 11, 13, 14, 15, 16, 16b). Door `{werkvragen_html}` binnen het bestaande `no-break`-blok te zetten reist het werkvragenblok mee zodra het navy vlak niet meer past; daarmee verdwijnen alle acht.
- **Twee `paginavulling`-bevindingen blijven open na Taak 6** (gemeten in het productie-image, WeasyPrint 70.0, na de wijziging hierboven): scenario **14 (Loep Behoud, veel Anders): pagina 13 is 35% gevuld**, het slotblok werkvragen + gespreksopener van de agenda; scenario **20 (Loep Start): pagina 12 is 32% gevuld**, de appendixstaart die de besluitpagina van de agenda afsnijdt. Beide zitten in dezelfde band als de drie bevindingen die al vóór plan 3b bestonden (26 tot 36%); de appendixstaart is precies het geval waarvoor Taak 8 een uitzondering toevoegt. `MIN_FILL` is niet veranderd en er is geen copy of opmaak opgerekt om een percentage te halen.
- **Testfixture-correctie in Taak 5 (geen copy- of ontwerpafwijking):** de wiringtest in het plan gaf `_prioriteringsraster` `n_total=13` naast de `DIRECTION`-fixture, waarvan de laagste-onderwerptelling 17 is. Dat is rekenkundig onmogelijk en `_direction_totals_line` weigert het terecht met een `ValueError`. De test gebruikt daarom `n_total=17`, dezelfde waarde als `_render` in `tests/test_report_priority_render.py`. Geen gate, staffel of drempel is aangepast.
- **Dunne verdiepingsblokken (restpunt productie-image, Taak 9): de ladder loste scenario 01, 09 en 19 niet op; teruggedraaid naar trede 0.** `_verd_los`/`.verd-los` zijn gebouwd exact zoals gespecificeerd (dun blok = `_deepening_block` levert niets, dus `triggered=0`) en gemeten op alle 24 bestanden in het productie-image (WeasyPrint 70.0). Vóór de ingreep: 01 pagina 7 op 36% ("VERDIEPING: WERKDRUK EN HERSTELRUIMTE"), 09 pagina 7 op 26% ("VERDIEPING: CULTUUR EN PSYCHOLOGISCHE VEILIGHEID"), 19 pagina 7 op 36% ("VERDIEPING: BELONING EN EERLIJKHEID"). Na de trede-0-ingreep (pairwise `verd-los`): **exact dezelfde drie percentages, byte-identiek voor 01 en 19** (geen van de drie prioritaire onderwerpen in die scenario's heeft `triggered=0`; ze tonen alle drie de "te weinig antwoorden"-terugval, wat een niet-leeg maar wel kort blok is en dus buiten de gespecificeerde definitie van "dun" valt) en **geen zichtbare verandering voor 09** (het echt dunne blok, Cultuur, kreeg wel `verd-los`, maar de resterende ruimte op de vorige pagina, gemeten op ongeveer 121pt/17% van de kolom, is kleiner dan het eerste onsplitsbare stuk van het blok (kop + score + spreidingsgrafiek, samengehouden via `break-after: avoid` en de bestaande `no-break` op het spreidingsblok), dus er is nergens een geldig breekpunt dat vroeger in de pagina valt). De trede-0-implementatie liet wel een echte bug zien: het letterlijk gespecificeerde `.verd-los { break-inside: auto; }` liet WeasyPrint bij drie *eerder OK* scenario's (02, 13, 15) de kaart "Welke toelichting respondenten kozen" van de voorganger loskoppelen van de rest van diens blok, met een 1-regelige kaart alleen op een nieuwe pagina (37%, net onder de grens) tot gevolg — geen regressie in de zin van de ladder, maar een direct gevolg van de letterlijke Stap 5-CSS. Opgelost binnen dezelfde trede met één extra regel (`.verd-los .card { break-before: avoid; }`, niet in het plan maar wel binnen de bedoeling van Stap 5: "binnendelen blijven heel"); daarmee terug op de Taak 8-baseline (0 regressies, 02/13/15 weer OK). **Trede 1** (hele hoofdstuk los zodra er één dun blok in zit) loste 09 op (16 in plaats van 17 pagina's) maar brak vier andere scenario's veel harder: 02, 13 en 15 kregen elk één losse tabelrij (met tekst als "Ik ervaar mijn werkdruk als acceptabel. LAAGSTE SCORE") alleen op een nieuwe pagina (1 tot 4% gevuld) en 14 kreeg er een tweede bevinding bij (26%) — de eerste, niet-dunne factor in die rapporten (met een volledige stellingtabel) werd nu ook `verd-los` en brak middenin zijn eigen tabel. Teruggedraaid volgens de ladderregel ("een winst op drie scenario's is geen verlies op een vierde waard"). **Trede 2** (`.verd-dun`, compactere binnenmaten, bovenop trede 0) verbeterde 09 licht (26% naar 23%, nog steeds NIET OK) maar voegde nieuwe bevindingen toe op 02, 13 en 15 (elk 39%, net onder de grens) en op een geheel nieuw scenario, 04 (37%); van 4 naar 8 bestanden met bevindingen. Ook teruggedraaid. **Besluit: gestopt bij trede 0 (inclusief de `break-before: avoid`-toevoeging).** `MIN_FILL` is niet aangepast, er is geen uitzondering toegevoegd en geen staffel of drempel versoepeld. Rapporten zonder dun blok zijn ongewijzigd. **Open voor Lars:** scenario 01, 09 en 19 blijven op 36%, 26% en 36% (dezelfde als vóór plan 3b); een structurele oplossing vraagt een ander mechanisme dan een CSS-breekregel op het verdiepingsblok zelf, bijvoorbeeld het laten meestromen van het volgende hoofdstuk (Werkbeleving) in plaats van diens vaste `.pb`, of het combineren van meerdere dunne "te weinig antwoorden"-blokken tot één blok in de data-laag. Beide zijn een groter herontwerp dan Taak 9 beoogt.
- **Opvolgtaak na Taak 7-review: lange MT-tekstvelden op de besluitpagina worden begrensd weergegeven, met een zichtbare melding.** De review vlagde terecht dat lege velden een vaste regelbegroting hebben (`_bl_lines`), maar een gevuld veld tot dan toe onbegrensd rendert. Gemeten in het productie-image (WeasyPrint 70.0, 2026-09-20): een besluit waarin `primary_action`, `secondary_action`, `feedback_plan` en `success_criterion` elk op hun frontendlimiet zitten (`DECISION_LIMITS.action`/`.text` = 600 tekens, `frontend/lib/dashboard/campaign-decision.ts`) en beide datums gevuld zijn, duwt de besluitpagina over een tweede vel in alle drie de producten (exit/retention/onboarding: 12/13/11 pagina's i.p.v. 11/12/10, `besluit-op-een-a4` twee bevindingen per bestand: de voetregel mist en de volgende pagina begint niet met een hoofdstukkop). Bij 470 tekens per lang veld past het net, bij 480 niet meer (gemeten met een herhaalde-woordfixture op Loep Behoud). **Oplossing (toegestaan, eerlijk):** nieuwe `BESLUIT_TEKST_MAX = 300` en `_bl_kort()` in `backend/report_html.py` knippen elk van de vier lange velden bij weergave af op een woordgrens binnen 300 tekens (ruime marge onder de gemeten knik van 470–480, voor natuurlijke tekst die anders wrapt dan de fixture) en voegen `"..."` toe; is minstens één veld ingekort, dan toont de pagina één keer de regel `BESLUIT_INGEKORT` ("Dit vel toont het begin van lange antwoorden; het volledige besluit staat in het dashboard."), boven de tabel, náást de bestaande status-regel. De opgeslagen waarde in `campaign_decisions` en de weergave in het dashboard zijn ongewijzigd; alleen de PDF-weergave is begrensd. Geen stille afkap (Fail Loud), geen limiet in de frontend aangepast, geen versoepeling van `besluit-op-een-a4`. Na de fix: alle drie de producten `besluit-op-een-a4` OK met een maximaal ingevuld besluit (11/10/12 pagina's, exit/onboarding/retention). Twee resterende `paginavulling`-bevindingen op datzelfde bestand (werkvragenblok "Zo maak je er een besluit van" op 35–38%, exit pagina 8, retention pagina 9) zijn niet gerelateerd aan het besluit (dat blok leest geen `decision`-data) en bestaan al in de reguliere fixture van `tests/test_report_degraded_page_two.py` (die `deepening_agg`/`direction_agg` leeg laat); buiten scope van deze opvolgtaak.
- **De twee openstaande `paginavulling`-bevindingen van Taak 6 zijn alsnog gesloten.** Scenario 20 (Loep Start, appendixstaart 32%) valt onder de uitzondering die Taak 8 toevoegde (`APPENDIX_STAART_UITGEZONDERD`). Scenario 14 (Loep Behoud, veel Anders, pagina 13 op 35%) is gefixt na de eindmeting: `.dir-block` had `break-inside: avoid` op het hele blok, inclusief de platte inleidende tekst, waardoor de sectie "Wat er moet gebeuren" bij een grote startpuntkaart een vers vel opende en er voor het eveneens ondeelbare agendaslot te weinig ruimte overbleef. Nu draagt alleen `.dir-grid` (de kaartentabel) `break-inside: avoid` en mag de inleiding meestromen; daarnaast is de regelafstand van `.anders-list li` krapper (1.5 naar 1.35, marge 3px naar 1px). Gemeten in het productie-image: scenario 14 van 18 naar 17 pagina's, `check_pdf_report` op alle regels OK, en geen van de twintig al passerende scenario's brak. Eindstand op alle 24 bestanden: **3 met bevindingen (01, 09, 19), alle drie pre-existent en hierboven verantwoord**, 0 warnings en 0 streepjes overal.
- **Labelconsistentie tussen dashboard en PDF (na de eindreview):** het dashboardformulier noemde het eerste veld "Onderwerp" terwijl de besluitpagina in de PDF "Startpunt" zegt, voor dezelfde `primary_topic` en dezelfde lezer. Het dashboard volgt nu de PDF ("Startpunt"), zodat het paar Startpunt/Tweede punt in beide lagen gelijk is. Alleen het label is gewijzigd, niet het veld, de validatie of de opslag.
